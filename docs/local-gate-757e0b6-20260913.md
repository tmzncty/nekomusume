# Developer-local exact-tree provenance — `757e0b6` bench P95 alignment

Exact pushed source SHA: `757e0b6c056f0bac39c1fb00e24d54cab55f3f72` (`fix(bench): align P95 order statistic with repository netns convention`).

Developer-local clean exact-tree gate for the bench percentile-consistency repair. Hosted GitHub checks are separate cross-evidence; this is the developer-local anchor. Not a WAN/live claim, not an independent security/release approval.

## Commands run on a clean worktree of the exact SHA

```bash
cargo test -p neko-bench
PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh
git diff --check
git status --short
```

## Result

- UTC start `2026-09-13T02:49:15Z`, UTC end `2026-09-13T02:51:15Z`.
- `cargo test -p neko-bench`: exit 0 (3 tests, including the new `p95_order_statistic_matches_repository_convention`).
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`: exit 0.
- `git diff --check`: exit 0.
- `git status --short` before/after: empty (clean tree throughout).
- Host `Linux 6.8.0-137-generic x86_64 GNU/Linux`; `rustc 1.98.0 (88d9e12ae 2026-08-18)`; Cargo.lock unchanged.

`READY_LIVE: none` and all release flags are unchanged by this provenance note.
