# Canonical vector review coverage (generated)

> **Non-normative review artifact.** Generated from `fixtures/canonical-vectors.v1.json`; it records review coverage and implementation evidence only. It does not add, replace, or freeze protocol requirements. `freeze=false` remains authoritative.

- Corpus schema: `nekomusume.canonical-vector.v1` revision `1`
- Corpus identity: `84a49313974341b93d17415d2fffec2b2d0a68fb009f2a6b32381ad65ba93184`
- Vector count: **42**
- Generator: `scripts/generate-canonical-review.py`

| # | id | domain / operation | classification | bytes | oracles | expected semantic fields / error | adapter / coverage path |
|---:|---|---|---|:---:|---|---|---|
| 1 | `negotiation.hello.v0-v2` | `negotiation` / `client_hello` | valid | present | encode, decode bytes equals expected, roundtrip | fields: `versions, selected` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (VersionNegotiator::client_hello)` |
| 2 | `negotiation.no-overlap` | `negotiation` / `server_accept_hello` | out_of_range, expected_failure | present | decode bytes equals expected | error: `NoCompatibleVersion` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (VersionNegotiator::server_accept_hello)` |
| 3 | `negotiation.duplicate` | `negotiation` / `server_accept_hello` | duplicate, expected_failure | present | decode bytes equals expected | error: `DuplicateOffered` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (VersionNegotiator::server_accept_hello)` |
| 4 | `negotiation.response.selected-0` | `negotiation` / `server_response` | valid | present | encode, decode bytes equals expected, roundtrip | fields: `selected` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (VersionNegotiator::client_accept_response)` |
| 5 | `negotiation.response.malformed` | `negotiation` / `server_response` | malformed, expected_failure | present | decode bytes equals expected | error: `Malformed` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (VersionNegotiator::client_accept_response)` |
| 6 | `negotiation.response.unsupported` | `negotiation` / `server_response` | unknown_version, expected_failure | present | decode bytes equals expected | error: `UnsupportedSelected` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (VersionNegotiator::client_accept_response)` |
| 7 | `wire.record-data` | `wire` / `record` | valid | present | encode, decode bytes equals expected, roundtrip | fields: `type, flags, payload_hex` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode,decode})` |
| 8 | `wire.record-ack` | `wire` / `record` | valid | present | encode, decode bytes equals expected, roundtrip | fields: `type, flags, payload_hex` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode,decode})` |
| 9 | `wire.record-path-challenge` | `wire` / `record` | valid | present | encode, decode bytes equals expected, roundtrip | fields: `type, flags, payload_hex` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode,decode})` |
| 10 | `wire.bad-version` | `wire` / `record` | unknown_version, expected_failure | present | decode bytes equals expected | error: `UnknownVersion` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode,decode})` |
| 11 | `wire.truncated` | `wire` / `record` | truncated, expected_failure | present | decode bytes equals expected | error: `LengthMismatch` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode,decode})` |
| 12 | `error.trailing` | `error` / `decode` | trailing, malformed, expected_failure | present | decode bytes equals expected | error: `TrailingBytes` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::decode)` |
| 13 | `frame.data` | `frame` / `frames` | valid | present | encode, decode bytes equals expected, roundtrip | fields: `frame_count, frames, payload_bytes` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 14 | `frame.datagram-small` | `frame` / `frames` | valid, integer_min | present | encode, decode bytes equals expected, roundtrip | fields: `frame_count, frames, payload_bytes` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 15 | `frame.datagram-max-1024` | `frame` / `frames` | valid, integer_max | present | encode, decode bytes equals expected, roundtrip | fields: `frame_count, payload_bytes, frames` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 16 | `frame.delivery-ack` | `frame` / `frames` | valid | present | encode, decode bytes equals expected, roundtrip | fields: `frame_count, frames, payload_bytes` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 17 | `close.empty` | `close` / `frames` | valid | present | encode, decode bytes equals expected, roundtrip | fields: `frame_count, frames` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 18 | `frame.path-challenge` | `frame` / `frames` | valid | present | encode, decode bytes equals expected, roundtrip | fields: `frame_count, frames, payload_bytes` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 19 | `frame.path-response` | `frame` / `frames` | valid | present | encode, decode bytes equals expected, roundtrip | fields: `frame_count, frames, payload_bytes` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 20 | `frame.unknown-ignorable` | `frame` / `frames` | valid, unknown_enum | present | encode, decode bytes equals expected, roundtrip | fields: `frame_count, frames, payload_bytes` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 21 | `frame.unknown-critical` | `frame` / `frames` | unknown_enum, expected_failure | present | decode bytes equals expected | error: `UnknownCritical` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 22 | `frame.reserved` | `frame` / `frames` | unknown_enum, expected_failure | present | decode bytes equals expected | error: `ReservedType` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 23 | `frame.truncated` | `frame` / `frames` | truncated, expected_failure | present | decode bytes equals expected | error: `Truncated` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 24 | `frame.oversized` | `frame` / `frames` | oversized, expected_failure | present | decode bytes equals expected | error: `LengthTooLarge` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 25 | `frame.count-max-64` | `frame` / `frames` | valid, integer_max | present | encode, decode bytes equals expected, roundtrip | fields: `frame_count, frames` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 26 | `frame.count-overflow-65` | `frame` / `frames` | oversized, expected_failure | present | decode bytes equals expected | error: `TooManyFrames` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_frames,decode_frames})` |
| 27 | `varint.boundary-0` | `wire` / `varint` | valid, integer_min | present | encode, decode bytes equals expected, roundtrip | fields: `value` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_varint,decode_varint})` |
| 28 | `varint.boundary-1` | `wire` / `varint` | valid, integer_max | present | encode, decode bytes equals expected, roundtrip | fields: `value` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_varint,decode_varint})` |
| 29 | `varint.boundary-2` | `wire` / `varint` | valid, integer_max | present | encode, decode bytes equals expected, roundtrip | fields: `value` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_varint,decode_varint})` |
| 30 | `varint.boundary-3` | `wire` / `varint` | valid, integer_max | present | encode, decode bytes equals expected, roundtrip | fields: `value` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_varint,decode_varint})` |
| 31 | `varint.boundary-4` | `wire` / `varint` | valid, integer_max | present | encode, decode bytes equals expected, roundtrip | fields: `value` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_varint,decode_varint})` |
| 32 | `varint.boundary-5` | `wire` / `varint` | valid, integer_max | present | encode, decode bytes equals expected, roundtrip | fields: `value` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_varint,decode_varint})` |
| 33 | `varint.noncanonical` | `wire` / `varint` | noncanonical_integer, malformed, expected_failure | present | decode bytes equals expected | error: `NonCanonicalInteger` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_varint,decode_varint})` |
| 34 | `varint.truncated` | `wire` / `varint` | truncated, expected_failure | present | decode bytes equals expected | error: `Truncated` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_varint,decode_varint})` |
| 35 | `varint.overflow` | `wire` / `varint` | integer_overflow, expected_failure | present | decode bytes equals expected | error: `IntegerOverflow` | `crates/neko-wire/tests/canonical_vectors.rs::every_claimed_oracle_executes_real_implementation_code (neko_wire::{encode_varint,decode_varint})` |
| 36 | `ack.range-max` | `ack` / `ack_range` | valid, integer_min, integer_max, state_only | state-only (no wire bytes) | none (state-only) | fields: `start, end` | `state-only exceptional row; no executable wire adapter claimed` |
| 37 | `reliable.packet-overflow` | `reliable_udp` / `packet_number` | integer_overflow, state_only | state-only (no wire bytes) | none (state-only) | error: `PacketNumberExhausted` | `state-only exceptional row; no executable wire adapter claimed` |
| 38 | `datagram.oversized` | `datagram` / `unreliable` | oversized, state_only | state-only (no wire bytes) | none (state-only) | error: `Oversized` | `state-only exceptional row; no executable wire adapter claimed` |
| 39 | `key-update.phase-one` | `key_update` / `update_key_phase` | valid, integer_max, state_only | state-only (no wire bytes) | none (state-only) | fields: `key_phase` | `state-only exceptional row; no executable wire adapter claimed` |
| 40 | `key-update.invalid-phase` | `key_update` / `update_key_phase` | unknown_enum, out_of_range, state_only | state-only (no wire bytes) | none (state-only) | error: `InvalidKeyPhase` | `state-only exceptional row; no executable wire adapter claimed` |
| 41 | `carrier.transition-generation` | `carrier_transition` / `path_validated` | valid, state_only | state-only (no wire bytes) | none (state-only) | fields: `path, generation` | `state-only exceptional row; no executable wire adapter claimed` |
| 42 | `carrier.transition-unauth` | `carrier_transition` / `path_validated` | unauthenticated, state_only | state-only (no wire bytes) | none (state-only) | error: `Unauthenticated` | `state-only exceptional row; no executable wire adapter claimed` |

## Gate contract

The generator is also the consistency gate: `python3 scripts/generate-canonical-review.py --check` regenerates this exact byte content and fails on drift. It validates that the corpus identity is embedded, every vector has one row, executable vectors have a known adapter mapping, and state-only exceptional rows explicitly have no wire adapter. The corpus validator and Rust canonical-vector test remain separate gates for schema, bytes, expected values, and real adapter execution.
