# Reviewer checkpoint — R9 P2 at exact `0d5d90c`

Date: 2026-09-16

## Scope

Independent bounded review of developer-owned source/test commit `0d5d90ce436024dbfa3f1c333ddc6f8cd4e33a86` (`test(cli): cross-process Carrier packet_number bind (H-R9-024 complete)`).

Inspected owners and applicable repository truth:

- `crates/neko-cli/src/main.rs` post-return reliable-UDP send / Recovery registration / dual-domain receive settlement;
- `crates/neko-cli/tests/probe.rs::reliable_udp_migration_back_reserves_final_record`;
- `README.md`, `AGENTS.md`, `ROADMAP.md`, `IMPLEMENTATION_PLAN.md`, `SECURITY.md`;
- `docs/status.md`, `docs/standing-vps-lab-authorization.md`, `docs/vps-rental-window-priority.md`, `docs/decisions.md`, `docs/carrier-architecture.md`, `docs/specs/nekomusume-session-v0.md`, `docs/release-security-review-packet.md`, and the prior `docs/CHATGPT_HANDOFF.md`.

This review challenges the evidence-domain invariant that the Carrier packet ACK for the post-return packet is mechanically bound to the exact Recovery packet number registered by the client, while Session DeliveryAck identity remains a separate stream/offset domain.

## Result — H-R9-024 closed narrowly

No new BLOCKER/HIGH was found in the `0d5d90c` repair.

The client now emits `r9_udp_post_return_sent` with `stream`, logical `offset`, and the exact `packet_number=pn` immediately after the same `pn` is admitted into the existing `ReliableUdpRuntime::on_packet_sent` owner. The server already emits `udp_return_packet_ack_sent(packet_number=post_pn)` using the packet number passed to `server_rt.on_packet_received`. The count=4 built-binary process fixture extracts those two Carrier-domain values and requires exact equality.

This closes the specific H-R9-024 cross-process identity gap. It does **not** by itself close P2 or R9-2: the remaining P2 C1-C4 exact identity/cardinality/order assertions, post-return ACK arrival-order challenge, P4 independent suppression negatives, and final developer-local exact-tree provenance are still dependency-ready work.

The test's numeric extraction uses an `u64::MAX` fallback on parse failure. Under the exact current diagnostic shape both sides publish a final decimal `packet_number` field and the process fixture is green, so this is not classified as a correctness finding for the current tree. Future diagnostic reshaping should prefer an explicit parse failure rather than sentinel equality.

## Exact-tree hosted cross-evidence

GitHub Actions run `35065452165` is attached to exact `0d5d90c` and completed successfully:

- `stable checks`: `bash scripts/check.sh` — success;
- `nightly decode fuzz smoke`: pinned `cargo fuzz build decode` and `cargo fuzz run decode -- -max_total_time=30 -max_len=8192` — success.

This is hosted cross-evidence only. It is **not** the required final developer-local clean exact-tree provenance for R9-2.

Open PRs at review time: none.

No WAN/VPS experiment was executed or inferred in this review. `READY_LIVE` remains none because the current dependency chain is local R9 correctness/evidence followed by independent cross-process R9 review.

## Remaining READY_LOCAL queue

Continue without waiting for reviewer cadence:

1. Finish P2 C1: exactly one client and server TCP replay confirmation for `seq=2`, stream 1, offset 32; preserve explicit negatives for offsets 0/16/48.
2. Finish P2 C2: make the selected recovery/validation/migration/post-return milestones one-only and strictly ordered; post-return send is exactly stream 1 / offset 48 and uses the already cross-bound packet number.
3. Finish P2 C3: exactly one server Session DeliveryAck at seq 3 / stream 1 / offset 48 / len 16 and exactly one Carrier packet ACK with the cross-bound packet number, after recovery owner start/validation.
4. Finish P2 C4: the authoritative client Session mutation event is `r9_udp_return_delivery_ack`; add/require len 16 there, require exactly one applied Carrier ACK and no rejected/false shortcut, then require exactly one settlement after both actual ACK-domain events with Recovery zero. Do not use `udp_return_delivery_ack_validated` as the mutation-order oracle.
5. Deterministically exercise both post-return ACK arrival orders through the same bounded authenticated receive owner and one operation deadline/malformed budget.
6. Tighten the two P4 suppression negatives independently: Session ACK present / Carrier ACK withheld, and Carrier ACK present / Session ACK withheld; each must positively observe the non-suppressed domain, terminate nonzero with the existing typed bounded failure, and emit no settlement/downstream success continuation.
7. Record final R9-2 developer-local clean exact-tree provenance on the final pushed source/test SHA (`PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`, `git diff --check`, clean initial/final tree, exact SHA/times/OS/arch/Rust stable/exit codes).
8. Continue immediately through R9-3 .. R9-12, then a dedicated independent bounded challenge of the materially new cross-process R9 integration surface, followed by factual release/item-4 reconciliation.

Do not change Session/Carrier/ACK/crypto/wire architecture, D019, capacity/TTL/LRU/history values, release authority, or production policy while closing these evidence slices.
