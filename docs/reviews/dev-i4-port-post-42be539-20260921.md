# Developer bounded I4-PORT post-42be539 independent review — platform-scope closure

**Anchor:** exact source/test tree `42be539` + reviewer commits through `9c2c309`.
**Owners inspected:** `crates/neko-cli/tests/probe.rs` — `signal_term`
(external `kill` command), `process_resource_snapshot` (`/proc/<pid>`),
`ready_failover_server`/`ready_endpoint_rebind_server` helpers
(`Child::kill()`), and every test fn that calls them.

## Per-invariant coverage

| Invariant | Coverage |
|---|---|
| Unix-dependent tests/helpers explicitly platform-scoped | `signal_term` (POSIX `kill`) and `process_resource_snapshot` (`/proc`) are only called by `sigterm_after_ready`, `udp_listener`, `periodic_server_signal_cleanup`, `endpoint_rebind_real_sockets`, `endpoint_rebind_wrong_challenge` — all now `#[cfg(unix)]` |
| Unix-only lifecycle evidence not promoted to non-Unix evidence | `#[cfg(unix)]` compiles these fixtures out on non-Unix — they cannot claim portability or produce non-Unix evidence |
| cfg scoping does not remove platform-independent coverage | `Child::kill()` in `ready_failover_server`/`reliable_udp_incomplete_settlement` is `std::process::Child::kill` — a cross-platform Rust API, not POSIX `kill`; those tests remain un-gated |
| first-RC Linux target / release policy not broadened | only `#[cfg(unix)]` attributes added; no release-flag or platform-support change |

## Commands actually run

- `cargo test -p neko-cli --test probe sigterm` — `sigterm_after_ready` PASS (Linux, gated)
- `cargo test -p neko-cli --test probe rebind` — both `endpoint_rebind_*` PASS (Linux, gated)
- `cargo test -p neko-cli --test probe periodic_server_signal` — PASS (Linux, gated)
- static scan: every `signal_term`/`process_resource_snapshot`/`/proc/`/`setsid` callsite maps to a `#[cfg(unix)]` test or a non-test helper whose callers are all gated

Not run: full `probe` suite (timed beyond the 120s harness); non-Unix build
(this host is Linux only).

## Exclusions

Windows/macOS verification is not performed on this Linux host; `#[cfg(unix)]`
removes these fixtures there rather than proving them.

## Result

No unguarded Unix-only owner remains. All POSIX-signal/`/proc` fixtures are
`#[cfg(unix)]`-gated; platform-independent `Child::kill()` callers stay
un-gated. The repair contract is satisfied.

**READY_LIVE: none** — deterministic local evidence only.
