# Reviewer checkpoint — R9-2 P2 partial H-R9-024 repair at `4b07c60`

Exact reviewed developer-owned source/test anchor: `4b07c60c3ba35fa792f658527854316e13c73886` (`fix(cli): Carrier packet-ACK carries packet_number, not logical offset (H-R9-024)`). Parent reviewer handoff: `2a894370b22ae1cdf24d65b45ac971f6ec957379`.

## Classification

This commit is a narrow CLI diagnostic/process-test repair. It does not change Session, Carrier, ACK framing, crypto, wire, or policy values.

Changed owners:

- `crates/neko-cli/src/main.rs`: preserves the received post-return Recovery packet number as `post_pn`, emits it on `udp_return_packet_ack_sent`, and adds `len` to the server Session DeliveryAck diagnostic.
- `crates/neko-cli/tests/probe.rs`: changes the C3 packet-ACK assertion from logical offset to the presence of a `packet_number` field.

No decoder/parser/crypto framing source changed.

## Exact-tree hosted cross-evidence

GitHub Actions run `35061342726` on exact `4b07c60c3ba35fa792f658527854316e13c73886` completed successfully:

- `stable checks`: SUCCESS; hosted job ran `bash scripts/check.sh`.
- `nightly decode fuzz smoke`: SUCCESS; hosted job ran the pinned decode fuzz build and 30-second smoke.

This is hosted cross-evidence only, not the required final developer-local clean exact-tree R9-2 provenance. Open PRs at review time: none.

## Accepted narrow progress

The server side of H-R9-024 is now materially better:

- `server_rt.on_packet_received(post_pn, true)` and `udp_return_packet_ack_sent(packet_number=post_pn)` share the same Carrier packet identity;
- the packet-ACK diagnostic no longer mislabels logical Session `offset=48` as Carrier identity;
- `udp_return_delivery_ack_sent` now carries `stream`, `offset`, and `len`, preserving the separate Session-delivery evidence domain.

Keep these changes.

## H-R9-024 remains HIGH — cross-process Carrier identity is still unproven

### Invariant

The positive P2 process fixture must mechanically bind the client Recovery packet registered at `on_packet_sent` to the server Carrier packet ACK generated from `on_packet_received` for that same packet. Session logical offset is correlation metadata only; it is not Carrier packet identity.

### Exact current source/test truth

The client already derives the post-return packet number immediately before `rt.on_packet_sent(...)`:

```text
let pn = u64::from_be_bytes(sealed[..8].try_into().unwrap());
rt.on_packet_sent(pn, ...)
```

but `r9_udp_post_return_sent` still emits only logical `offset` and does not expose `packet_number=pn`.

The server now emits `udp_return_packet_ack_sent(packet_number=post_pn)`, but the process test merely checks that this server line contains a `packet_number` field. It does not compare that value with the client-owned packet number, because the client diagnostic still does not publish it.

Therefore the current fixture cannot distinguish the correct ACK from an ACK diagnostic carrying some other Recovery packet number. H-R9-024 is only partially repaired and must remain open.

### Smallest repair contract

Do not change ACK framing, receive ownership, Session semantics, wire format, crypto, or policy values.

1. At the existing client `pn` derivation / `on_packet_sent` point, add secret-free `packet_number=pn` to the one `r9_udp_post_return_sent` diagnostic. Keep stream/offset correlation metadata if useful.
2. In the count=4 P2 fixture, require exactly one client `r9_udp_post_return_sent` and exactly one server `udp_return_packet_ack_sent`, extract their `packet_number` values, and require exact equality.
3. Do not use logical `offset=48` as the Carrier-domain oracle. Keep Session DeliveryAck assertions independent.
4. Once cross-process packet identity is exact, continue immediately with the remaining C1-C4 evidence assertions below; do not stop at the next reviewer cadence.

## Remaining exact P2 obligations after H-R9-024

### C1 TCP replay identity/cardinality

The fixture has one-only client/server TCP DeliveryAck lines and offset-32 checks, but it still does not mechanically assert the full requested identity on both sides. Require exact `seq=2`, stream 1, offset 32 on client and server, and no replay/ACK evidence for offsets 0/16/48.

### C2 migration -> post-return ownership chain

The fixture proves `udp_migrated_back < r9_udp_post_return_sent` and one-only post-return send, but the full selected milestone cardinality/identity chain is not yet exact. Require one-only selected challenge/validated/migrated events, then exactly one post-return send for stream 1 / offset 48 / the client Recovery packet number from H-R9-024. Keep offset 48 absent from uncertain/TCP/pre-promotion reliable ownership evidence.

### C3 server dual-domain evidence

After the packet-number cross-bind exists:

- exactly one Session DeliveryAck: `seq=3`, stream 1, offset 48, len 16;
- exactly one Carrier packet ACK with the exact packet number matching the client Recovery owner;
- strict owner-started -> validated -> Session DeliveryAck -> Carrier packet ACK order.

The current test only checks `offset=48` on the Session DeliveryAck line and only field presence on the packet-ACK line.

### C4 client dual-domain settlement

The actual Session-confirmation event `r9_udp_return_delivery_ack` remains the correct ordering point, but it currently emits/tests only stream/offset. Add/assert `len=16`, retain exactly-one semantics, and keep the Carrier event exactly once with `applied=true` and no rejected/false shortcut.

For terminal ordering, compare `r9_udp_post_return_settled` against the actual `r9_udp_return_delivery_ack` mutation event and actual `r9_udp_return_packet_ack` event. Do not use the later `udp_return_delivery_ack_validated` post-loop summary as the Session-domain transition point.

## Continuous queue

1. Finish H-R9-024 cross-process Carrier packet identity.
2. Finish exact C1/C2 identity/cardinality/order in the existing count=4 fixture.
3. Finish exact C3/C4 Session fields, Carrier identity, actual-event ordering, and one-only settlement.
4. Exercise both post-return ACK arrival orders through the same bounded authenticated receive owner, single absolute deadline, and single malformed budget.
5. Tighten both P4 ACK-domain suppression negatives with positive observation of the non-suppressed domain and typed terminal no-continuation assertions.
6. Record final developer-local clean exact-tree R9-2 provenance (`PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`, `git diff --check`, clean initial/final tree, exact reachable pushed SHA, UTC/OS/arch/Rust/exit codes).
7. Continue immediately through R9-3 data-loss recovery, R9-4 ACK-loss/reorder, R9-5 adversarial feedback atomicity, R9-6 ownership/resource boundedness, R9-7 process/result truth, R9-8/R9-9 health promotion, R9-10 uncertain replay/cleanup, and R9-11/R9-12 coherent gate.
8. Then perform Q10 dedicated independent cross-process R9 integration review and only afterward Q11/Q12 factual status/release-packet reconciliation.

## Release/live boundary

`READY_LIVE: none` remains authoritative. Standing VPS authorization remains valid, but this checkpoint creates no new dependency-satisfied real-network question. Release item 3 and item 4 remain incomplete; `RELEASE_CANDIDATE=false`, `PRODUCTION_READY=false`, `FREEZE=false`, and `RELEASED=false` remain unchanged.
