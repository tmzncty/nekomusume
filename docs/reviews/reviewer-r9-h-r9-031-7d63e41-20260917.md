# Reviewer checkpoint — H-R9-031 post-return ACK process-oracle gap

Date: 2026-09-17
Reviewed developer tree: exact `7d63e41f077ad451082a46c0c7572409a6cd204f`
Previous reviewer handoff anchor: `f5183d50aa1cc81f825d4df849162bf5c989d0ca`
Classification: **HIGH / evidence-correctness blocker for R9-2 acceptance**

## Scope

Independent bounded review of the two developer-owned commits after the prior handoff:

- `1abe30c52b73de6107cad027a396d94cca642415` — implementation + process-test change for H-R9-030;
- `7d63e41f077ad451082a46c0c7572409a6cd204f` — formatting-only follow-up.

Inspected exact-current owners:

- `crates/neko-cli/src/main.rs` post-return reliable-UDP ACK production and dual-domain settlement path;
- `crates/neko-cli/tests/probe.rs` post-return stale/future process fixtures;
- `docs/CHATGPT_HANDOFF.md` acceptance contract;
- current Session/Carrier evidence-separation architecture/spec and release/item-4 boundaries.

This review does not claim a fresh local gate. Hosted GitHub Actions run `35139025227` on exact `7d63e41` completed successfully; both `stable checks` (`bash scripts/check.sh`) and `nightly decode fuzz smoke` were green. Hosted CI is cross-evidence only.

## H-R9-030 source seam: accepted narrowly

The exact-current server now does the important ownership repair correctly:

1. authenticated post-return Data reaches Session receive;
2. the reliable receiver records the exact post-return packet number;
3. `poll_outgoing_ack(0)` is called once and its canonical ACK plaintext is retained;
4. stale mode sends the legitimate current Carrier ACK first and then re-seals the same retained plaintext as one semantic duplicate while Session confirmation is still outstanding;
5. future mode sends one authenticated never-sent ACK before the legitimate current Carrier ACK;
6. ordinary no-seam P2 keeps Session DeliveryAck before Carrier ACK.

No contradictory runtime correctness defect was found in this bounded source diff. The prior ACK-obligation-consumption defect H-R9-030 is therefore source-closed at exact `7d63e41` absent contradictory later evidence.

## H-R9-031 HIGH — the newly unignored process tests are not discriminating enough

The two post-return fixtures are now unignored, but they do not satisfy the existing handoff acceptance contract and can still pass after a partial/failed process execution.

### Stale fixture

`reliable_udp_post_return_stale_ack_is_accepted_empty` currently:

- ignores the returned client exit status;
- ignores the returned server exit status and server log;
- only requires that stdout contains one accepted-empty event string;
- only requires absence of the post-return rejected event string;
- only requires that a settled event string appears at least once.

It does **not** prove:

- client and server success;
- exactly-one accepted-empty classification;
- exactly-one true current-packet retirement;
- exact client/server packet-number equality for that retirement;
- exact Session confirmation `stream=1 offset=48 len=16`;
- final Recovery `in_flight=0` at settlement;
- exactly-one settlement;
- that the duplicate did not manufacture Session delivery or bypass the dual-domain completion gate.

A client can emit the expected accepted-empty string and then fail later; the test still passes. The same is true for a server failure after the injected seam.

### Future fixture

`reliable_udp_post_return_future_ack_is_rejected` is weaker still. It currently ignores both process statuses and only checks that stdout contains a post-return rejected-event string. It does **not** prove:

- exactly-one typed rejection;
- no false current-packet retirement attributable to the injected future ACK;
- subsequent legitimate current Carrier ACK retirement;
- exact Session confirmation;
- exact cross-bound packet identity;
- final Recovery zero;
- exactly-one settlement;
- client/server success.

Therefore exact `7d63e41` demonstrates that the new seams execute under hosted tests, but it does not yet provide acceptance-grade process proof for H-R9-029/H-R9-030.

## Required smallest repair

Do not change Session/Carrier/ACK/wire/crypto semantics and do not invent new policy values. Strengthen only the existing two built-binary fixtures using already-emitted diagnostics:

1. require client success and server success, with stdout/stderr included in assertion failures;
2. parse/count exact diagnostic events rather than `contains`-only checks;
3. stale case: exactly one post-return accepted-empty, zero post-return rejected, exactly one positive Carrier retirement for the exact post-return packet number, exact Session transition `stream=1 offset=48 len=16`, exactly one settlement with `remaining_in_flight=0`, settlement after both true ACK-domain transitions;
4. future case: exactly one typed post-return rejection, then exactly one positive retirement for the real post-return packet, exact Session transition, exactly one settlement with Recovery zero; explicitly reject any positive retirement before the legitimate current ACK identity is observed;
5. cross-bind client `r9_udp_post_return_sent(... packet_number=<pn>)`, server `udp_return_packet_ack_sent(packet_number=<pn>)`, and client positive retirement to the same `<pn>`;
6. keep the current source ordering and one-poll ACK obligation seam unless the stronger fixture exposes a concrete contradiction.

Focused process tests are sufficient for this repair. No decoder/parser/framing semantic change is implied; fuzz is not required solely because these assertions are strengthened. Final R9-2 developer-local exact-tree provenance remains a later dedicated gate.

## Queue consequence

R9-3 remains blocked on acceptance-grade closure of H-R9-031 plus the still-open initial-path one-shot H-R9-028 work. After H-R9-031, continue without waiting:

H-R9-028 -> P2 C1-C4 exact closure -> both post-return ACK arrival orders -> P4 single-domain suppression negatives -> R9-2 developer-local exact-tree provenance -> R9-3..R9-12 -> dedicated independent cross-process R9 review -> Q10/Q11/Q12 reconciliation.

`READY_LIVE` remains none. This finding creates no new real-network question; repeating existing VPS evidence is not justified.
