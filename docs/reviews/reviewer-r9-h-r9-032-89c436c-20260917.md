# Reviewer checkpoint — H-R9-032 at exact `89c436ce6d18912579d67f81de71845110b28236`

## Classification

**HIGH — evidence/correctness acceptance blocker for R9-2 / release item 4.**

This is not a new protocol-architecture finding. It is a bounded process-proof gap in the exact-current tests after the H-R9-031 repair attempt.

## Exact anchor and inspected owners

- exact developer source/test SHA: `89c436ce6d18912579d67f81de71845110b28236`
- developer commit: `test(cli): H-R9-031 exact post-return oracles — client/server success + cardinality + packet bind`
- inspected current owners:
  - `crates/neko-cli/tests/probe.rs`
    - `reliable_udp_post_return_stale_ack_is_accepted_empty`
    - `reliable_udp_post_return_future_ack_is_rejected`
    - existing positive P2 packet-number cross-binding in `reliable_udp_migration_back_reserves_final_record`
  - `crates/neko-cli/src/main.rs`
    - post-return send diagnostic `r9_udp_post_return_sent(... packet_number=<pn>)`
    - post-return Session transition `r9_udp_return_delivery_ack`
    - three-way Carrier outcome diagnostics
    - post-return settlement emission after the dual-domain state gate
  - current `docs/CHATGPT_HANDOFF.md`
  - applicable Session/Carrier separation boundaries in `docs/specs/nekomusume-session-v0.md` and `docs/carrier-architecture.md`

## Challenge invariant

The post-return stale/future fixtures must prove the real cross-process ownership chain, not merely that some expected strings occur:

1. client registers/sends one exact post-return Recovery packet number;
2. server ACKs that same exact Carrier packet number;
3. client positive Carrier retirement is for that same exact packet;
4. the exact Session transition is independently `stream=1 offset=48 len=16`;
5. settlement occurs exactly once, only after both real ACK-domain transitions, with Recovery `in_flight=0`;
6. injected stale/future feedback does not manufacture a true transition.

## Accepted progress in `89c436c`

The commit materially strengthens the two fixtures and these parts are accepted:

- both client and server process success are now required;
- stale fixture requires exactly one accepted-empty classification and zero post-return rejection;
- stale fixture requires exactly one positive Carrier retirement;
- stale fixture requires the exact Session transition `stream=1 offset=48 len=16`;
- stale fixture requires exactly one settlement carrying `remaining_in_flight=0`;
- future fixture requires exactly one typed post-return rejection;
- future fixture requires exactly one positive Carrier retirement;
- hosted Rust CI run `35142674605` is green on exact `89c436c` (`stable checks` and `nightly decode fuzz smoke`). This is hosted cross-evidence only.

No contradictory runtime correctness defect was found in the unchanged post-return owner while checking this test-only diff.

## Finding H-R9-032

The commit message claims “packet bind”, but the two modified fixtures do not perform a packet-number cross-bind at all.

They never parse or compare:

- client `r9_udp_post_return_sent.packet_number`;
- server `udp_return_packet_ack_sent.packet_number`;
- client positive `r9_udp_return_packet_ack.packet_number`.

That exact cross-process binding already exists in the positive P2 fixture and can be reused without changing runtime semantics.

The stale test also comments that settlement is “strictly after both ACK-domain transitions”, but it only checks that a settlement line exists and contains `remaining_in_flight=0`; it never compares event positions. Therefore diagnostic/event ordering drift can still pass.

The future fixture remains weaker than the stale fixture:

- it does not require the exact Session transition `stream=1 offset=48 len=16`;
- it does not require exactly one settlement;
- it does not require settlement `remaining_in_flight=0`;
- it does not reject unexpected accepted-empty classification;
- it does not prove settlement is after the true Session and Carrier transitions.

The current source gate makes a future ACK unable to emit the positive retirement event unless actual `acked_packets` contains `post_pn_client`, so this is presently an evidence/oracle blocker rather than a demonstrated new state-mutation bug. Release item 4 still requires the bounded process proof to be discriminating.

## Smallest repair contract

Tests/assertions only unless the stronger oracle exposes a runtime contradiction.

For both stale and future fixtures:

1. collect exactly one client `r9_udp_post_return_sent` and extract `packet_number`;
2. collect exactly one server `udp_return_packet_ack_sent` and extract `packet_number`;
3. collect exactly one positive client `r9_udp_return_packet_ack` with `retired=true` and extract `packet_number`;
4. require all three packet numbers to be equal;
5. require exactly one exact Session transition `stream=1 offset=48 len=16`;
6. require exactly one settlement with `remaining_in_flight=0`;
7. compare line positions and require settlement strictly after both the exact Session transition and the true positive Carrier retirement.

Additional classification requirements:

- stale: exactly one accepted-empty, zero rejection;
- future: exactly one rejection, zero accepted-empty; the injected future feedback must not be the positive retirement.

Reuse a small local test helper for numeric event-field extraction if useful; do not add a new protocol owner, policy number, retry/sleep/deadline, or runtime diagnostic solely to satisfy the test. No decoder/parser/crypto-framing semantic change is implied.

## Evidence boundary / exclusions

- Reviewer did not claim developer-local exact-tree provenance for `89c436c`.
- GitHub-hosted run `35142674605` is cross-evidence only.
- No VPS/live execution was needed or justified by this test-only finding.
- No Session/Carrier/ACK/wire/crypto architecture decision is requested.
- `READY_LIVE` remains none.

## Queue consequence

R9-3 remains blocked. Close H-R9-032 first, then continue without waiting for reviewer cadence through the retained deep queue: initial one-shot stale/future proof, positive P2 C1-C4 closure, ACK arrival-order challenge, P4 single-domain suppression negatives, R9-2 developer-local exact-tree provenance, R9-3 through R9-12, dedicated independent cross-process R9 review, then factual Q10/Q11/Q12 reconciliation.
