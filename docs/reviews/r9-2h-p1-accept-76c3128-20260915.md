# R9-2H P1 independent bounded acceptance — exact `76c31284f10920728f6f5609cefa665cf1c3a68b`

## Scope

Independent bounded review of the built-process reversed Session DeliveryAck evidence requested by `docs/CHATGPT_HANDOFF.md` P1. This is an evidence/test review only; it is not a protocol freeze, security approval, WAN result, release decision, or completion of R9-2H.

Reviewed owners:

- `crates/neko-cli/tests/probe.rs::reliable_udp_reversed_ack_order_confirms_in_order`
- the already accepted R9-2H source invariant that logical `outstanding + pending_acks` retires before Carrier-only settlement
- current Session/Carrier ACK separation from `AGENTS.md`, `docs/carrier-architecture.md`, and `docs/specs/nekomusume-session-v0.md`

## Verdict

**P1 ACCEPT. BLOCKER/HIGH: none found in this bounded evidence slice.**

Exact `76c3128` closes the remaining P1 evidence gaps:

- the built client process must exit success;
- exactly one structured `r9_udp_delivery_ack_buffered` event exists for `offset=16` while `watermark=0`;
- the covered shortcut is forbidden;
- exactly two structured applied `r9_udp_delivery_ack_validated` events exist and their line order is exact: stream 1 / offset 0 first, then stream 1 / offset 16 with `buffered=true`;
- the terminal structured settlement event carries `remaining_in_flight=0`.

The order proof is no longer based on generic offset substrings. The first applied-event line is pinned by the event name and the `applied_events` vector independently proves its exact offset/stream identity and that the second applied event is the buffered offset-16 confirmation. The unique buffered event is also identity-checked.

The existing source boundary remains controlling: logical outstanding/pending confirmation ownership must retire before Carrier-only settlement, and the successful terminal `r9_udp_in_flight_settled` event is only reachable after that boundary and Recovery settlement. P1 does not require a new protocol or logging framework.

## Verification/evidence truth

- GitHub-hosted `stable checks` on exact `76c3128`: success.
- GitHub-hosted `nightly decode fuzz smoke` on exact `76c3128`: success.
- These hosted checks are supplementary evidence only. This reviewer did not claim a developer-local clean exact-tree gate for `76c3128`.
- Final developer-local R9-2H provenance remains required after P2-P4 land on one reachable source/test SHA.

## Exclusions / next dependency

Not reviewed or closed here:

- P2 reserved post-migration reliable ownership/dual settlement evidence;
- P3 operation-wide malformed-budget behavior across valid Carrier feedback;
- P4 incomplete settlement terminality, especially post-return;
- R9-3+ loss/PTO/retransmit/TCP-promotion work;
- WAN/VPS evidence (`READY_LIVE: none` remains controlling until later reconciliation).

The next dependency-ready slice is P2. The current P2 process test is still too weak: it explicitly tolerates a nonzero client exit and only proves absence of the legacy `udp_uncertain_range_sent` for reserved offset 32. It does not yet prove post-promotion reliable send ownership, independent Session + Carrier acknowledgements, dual settlement to `in_flight=0`, or both acknowledgement orders.
