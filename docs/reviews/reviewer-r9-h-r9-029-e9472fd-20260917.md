# Independent reviewer checkpoint — H-R9-029 source repair at `e9472fd`

Date: 2026-09-17

Reviewed exact developer source/test anchor: `e9472fd56b12c080c1a84553a6a7e4007f8b9f70` (`fix(cli): post-return Carrier ACK three-way outcome (H-R9-029)`).

## Scope

Bounded independent review of the post-return reliable-UDP Carrier-ACK outcome path and its existing process-test coverage. Inspected:

- `crates/neko-cli/src/main.rs` post-return migration-back receive owner and `UdpAcknowledgement::Carrier` consumer;
- `crates/neko-cli/tests/probe.rs` current reliable-UDP stale/future regressions and P2/P3/P4 neighborhood;
- current `docs/CHATGPT_HANDOFF.md` acceptance contract;
- current release/status, standing VPS authorization, carrier/session architecture/spec, decisions and security/release packet boundaries.

Excluded from this checkpoint: new live WAN work, capacity/pressure benchmarking, release-authority decisions, D019/policy values, and redesign of Session/Carrier/ACK/wire/crypto semantics.

## Developer commit classification

`e9472fd` is an implementation-only source change in `crates/neko-cli/src/main.rs`; it does not add or modify process tests, fixtures, release docs, or live evidence.

## H-R9-029 source result

The source-side three-way classification is directionally correct and closes the stale handoff claim that the post-return consumer discarded `rejected`:

- positive current-packet evidence is emitted only for `applied && acked_packets.contains(post_pn_client)`;
- typed Recovery rejection emits `r9_udp_return_packet_ack_rejected` without claiming current-packet retirement;
- accepted non-retiring feedback emits a classification-only `r9_udp_return_packet_ack_accepted_empty` event;
- the dual-domain completion gate remains based on actual Session outstanding state plus `rt.in_flight()==0`, so these diagnostics do not themselves manufacture success.

No contradictory source defect was found in the bounded `e9472fd` diff.

## H-R9-029 remains acceptance-blocked on focused process evidence

The source repair has no matching built-binary post-return stale/future regression in exact `e9472fd`. In particular, current `probe.rs` contains no assertion for `r9_udp_return_packet_ack_accepted_empty` on the migration-back post-return path.

Before H-R9-029 is closed, add deterministic authenticated post-return cases using the existing receive owner/deadline/budget:

1. fresh-envelope stale semantic Carrier ACK after migration-back: exactly one accepted-empty classification, zero rejection, no false positive current-packet retirement, later exact legitimate Carrier retirement + exact Session confirmation, final Recovery zero, client/server success;
2. future/never-sent Carrier ACK after migration-back: exactly one typed rejection, no false current-packet retirement, later legitimate Carrier progress + exact Session confirmation, final Recovery zero, client/server success.

Do not add another receive owner, ACK architecture, deadline, malformed budget, capacity value, or protocol field for this proof.

## H-R9-028 remains open

The earlier initial-path stale/future regressions are still not sufficiently discriminating at this anchor:

- the server stale/future injection remains located in the ordinary packet-ACK emission path rather than being proven one-shot for the bounded operation;
- the stale process test proves absence of rejection plus eventual Recovery zero, but does not require an observed accepted-empty classification;
- the future process test requires a rejection event but does not yet pin exactly-one injection/rejection and subsequent legitimate progress.

Keep the existing H-R9-028 contract queued immediately after the post-return H-R9-029 evidence slice.

## CI / provenance boundary

GitHub-hosted Rust CI run `35124075507` completed successfully on exact `e9472fd`. This is hosted cross-evidence only; it is not the required developer-local clean exact-tree provenance. The developer-local final R9-2 gate remains outstanding.

`e9472fd` does not change decoder/parser/crypto framing semantics, so pinned decode fuzz is not newly required solely because of this source slice.

## Release / live classification

- Open PRs at review time: none.
- `READY_LIVE: none` remains correct; this review created no new unresolved real-network question.
- Release item 3 and item 4 remain incomplete; no RC/freeze/release/production authority state should change.
- Queue exhaustion is false because H-R9-029/H-R9-028 evidence work and the preserved R9-2 through R9-12 independent-review queue remain dependency-ready.

## Reviewer disposition

H-R9-029 source repair: **accepted directionally, not yet closed**.

R9-3: **still blocked** until H-R9-029 focused post-return evidence and H-R9-028 one-shot/discriminating initial stale/future evidence are both closed on reachable pushed source/test commits, followed by the required local exact-tree gate at the appropriate R9-2 closure point.
