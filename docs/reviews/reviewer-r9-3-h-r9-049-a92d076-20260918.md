# Independent bounded review — H-R9-049 remains open at exact `a92d076`

## Scope and anchor

- Reviewed developer-owned commit: exact `a92d07630835b63504278e4653aaa9590894598b`.
- Commit class: focused test-only change in `crates/neko-reliable/src/lib.rs`.
- Prior accepted source repair remains exact `1c3bf1d30e3089da230b2aedce54dbcecd6107f9`: the CLI post-return Carrier projection iterates every typed `acked_packets` identity and emits one positive `r9_udp_return_packet_ack` per retired packet.
- This review challenges only whether exact `a92d076` supplies the discriminator required to acceptance-close H-R9-049. It does not reopen the already accepted H-R9-048 source repair and does not claim a new Recovery or Session runtime defect.

## Inspected owners / claims

- `crates/neko-reliable/src/lib.rs::Recovery::on_ack` and the new test `one_ack_range_retires_multiple_outstanding_packets`.
- Exact `1c3bf1d` CLI projection diff in `crates/neko-cli/src/main.rs`.
- Current `docs/CHATGPT_HANDOFF.md` H-R9-049 acceptance contract.
- Provisional evidence-domain separation in `docs/specs/nekomusume-session-v0.md` and the Carrier/Session split in `docs/carrier-architecture.md`.

## What `a92d076` proves

The new deterministic unit test is useful and should remain. It constructs three outstanding Recovery packets, inserts one canonical ACK range covering packet numbers 1, 2 and 3, calls `Recovery::on_ack`, and requires the single typed transition to expose `acked_packets == [1, 2, 3]` with `in_flight() == 0`.

That closes a lower-layer premise: one canonical ACK can retire multiple packet identities, and the Recovery API exposes the complete typed retirement vector rather than a representative last identity.

## Why H-R9-049 is still open

The H-R9-049 contract is specifically a discriminator for the **actual post-return Carrier evidence projection**. Exact `a92d076` never executes `crates/neko-cli/src/main.rs`, never reaches `recv_udp_delivery_ack`, never emits or inspects `r9_udp_return_packet_ack`, and never compares the typed retirement set with the emitted diagnostic set.

Therefore the exact regression still does not distinguish the repaired `1c3bf1d` projection from the pre-repair representative-only implementation. Reintroducing `.last()` in the CLI projection could leave the new `neko-reliable` unit test green. The added test is necessary supporting evidence, but it is not the requested end-to-end or shared-helper discriminator.

This is an evidence/oracle correctness gap, not a newly proven state-machine defect.

## Required smallest next step

Keep both exact `1c3bf1d` source semantics and exact `a92d076` reliable-layer regression. Add the missing projection discriminator using the smallest current-tree seam:

1. make one canonical Carrier ACK reach the exact runtime/shared projection owner with a typed retirement set of cardinality at least two;
2. assert emitted positive `r9_udp_return_packet_ack.packet_number` values equal that typed retired-packet set in both set membership and cardinality;
3. assert no identity is omitted or duplicated and that the same ACK is not also projected as rejected or accepted-empty;
4. keep Session `DeliveryAck` separate and prove no second logical Session transition is created solely because multiple Carrier copies retire;
5. preserve truthful final Recovery/Session settlement.

If the existing process fixture cannot reach this without disproportionate churn, the already-authorized fallback remains valid: extract a tiny projection helper that the runtime itself calls, unit-test the helper with `acked_packets.len() >= 2`, and add a separate focused test proving `recv_udp_delivery_ack` forwards the complete typed vector unchanged. Do not redesign ACK architecture, wire format, crypto framing, or introduce policy values.

## Verification / provenance boundary

GitHub-hosted Rust CI for exact `a92d076`, run `35296911606`, completed successfully. This is cross-evidence only. It does not replace the missing projection discriminator or developer-local clean exact-tree provenance required after the final source/test SHA.

## Classification

- H-R9-049: **OPEN — HIGH / evidence-oracle correctness**.
- H-R9-048 source repair: retained.
- R9-3: remains blocked on H-R9-049, then H-R9-041 refusal regression and H-R9-042 pre-deadline negative.
- `READY_LIVE`: none; no new real-network hypothesis was created by this review.
