# Independent bounded review — H-R9-034 settlement accepted-empty classification

**Reviewed developer source/test anchor:** `b801b65cee8bfef196b6142ee6f5a9bfa876691a`

**Result:** no contradictory correctness/evidence defect found in this bounded seam; H-R9-034 may remain closed at exact `b801b65`.

## Scope inspected

- `crates/neko-cli/src/main.rs`
  - reliable-UDP server Carrier-ACK production and `--send-stale-ack-late` injection;
  - client initial authenticated ACK owner;
  - client settlement continuation using the same absolute application deadline;
  - accepted-empty / applied / rejected Carrier result classification.
- the built-binary regression `reliable_udp_stale_ack_settlement_phase_is_accepted_empty` introduced by exact `b801b65`;
- applicable layering/evidence boundaries in `AGENTS.md`, `docs/carrier-architecture.md`, and `docs/specs/nekomusume-session-v0.md`;
- the current rolling queue in `docs/CHATGPT_HANDOFF.md`.

This review is intentionally bounded to H-R9-034. It does not independently close the remaining positive P2 C1-C4 oracle gaps, ACK-arrival-order challenge, P4 one-domain-suppression negatives, or the complete cross-process R9 integration.

## Invariant challenged

For an authenticated Carrier ACK accepted by Recovery but retiring no packet (`applied=false`, `rejected=false`), process truth must not depend on whether the datagram is consumed by the initial receive loop or by its settlement continuation.

The accepted-empty class must therefore:

1. remain distinct from a real packet retirement and from typed rejection;
2. emit classification-only evidence in either continuation;
3. increment neither applied nor rejected counters and mutate neither Recovery nor Session state;
4. be demonstrably reachable through a semantic stale/duplicate ACK, not through an unauthenticated/crypto-rejected packet;
5. remain within the existing bounded owner and absolute deadline rather than creating a fresh unbounded wait.

## Source challenge

Exact `6af6468` repaired the missing settlement classification by emitting `r9_udp_packet_ack_accepted_empty` for the third Carrier outcome without changing Recovery/Session state or counters.

Exact `b801b65` supplies the discriminating seam and proof:

- the server retains the canonical Carrier-ACK plaintext for the first reliable record;
- only after the last reliable record's Session DeliveryAck does `--send-stale-ack-late` re-seal that same plaintext under a fresh authenticated envelope;
- the client settlement continuation is permitted one bounded final drain after Recovery reaches zero on the non-migration-back initial path, while retaining the same absolute application deadline;
- the settlement continuation classifies the resulting authenticated stale/duplicate as accepted-empty;
- the new process test requires both processes to succeed, exactly one accepted-empty classification, exactly two real applied Carrier ACKs, zero typed rejection, both exact initial Session confirmations, accepted-empty after both logical confirmations, and final `remaining_in_flight=0` evidence.

I did not find a source path in this bounded seam that promotes accepted-empty to Session delivery or packet retirement, increments an applied/rejected counter for it, or grants a fresh deadline.

## Verification provenance boundary

Reviewer evidence here is an independent source/oracle challenge, not a claim that this reviewer personally executed a local worktree gate.

Repository-persisted developer-local exact-tree provenance for pushed `b801b65` records:

- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` -> exit 0;
- `git diff --check` -> exit 0;
- clean worktree at the pushed SHA;
- `2026-09-16T23:12:11Z` through `2026-09-16T23:15:35Z`;
- Linux x86_64;
- `rustc 1.98.0 (88d9e12ae 2026-08-18)`.

Separate GitHub-hosted cross-evidence for exact `b801b65`, run `35161172315`, completed successfully:

- `stable checks`: `bash scripts/check.sh` passed;
- `nightly decode fuzz smoke`: pinned cargo-fuzz build and `cargo fuzz run decode -- -max_total_time=30 -max_len=8192` passed.

The H-R9-034 source/test change does not itself alter the decoder/parser/crypto framing grammar; the hosted fuzz result is additional cross-evidence, not a reason to redefine the local fuzz requirement.

## Exclusions / non-claims

This closure does **not** establish arbitrary post-zero datagram-drain semantics, WAN behavior, release readiness, production safety, protocol freeze, or security approval. It does not close R9-2 as a whole. The positive migration-back fixture still has exact C1-C4 oracle gaps, followed by the dual ACK arrival-order challenge and P4 one-domain suppression negatives.

`READY_LIVE` remains `none`; no new real-network question follows from this closure.
