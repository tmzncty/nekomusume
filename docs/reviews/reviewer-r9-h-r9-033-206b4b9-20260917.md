# Reviewer checkpoint — H-R9-033 initial stale ACK proof is still vacuous

Date: 2026-09-17

## Exact scope

- Developer source/test anchor reviewed: `206b4b936b1401c45bc0a23c730032f91eab8f0d`.
- Reviewer also read the later handoff-only commit `f63d3ff2563f0461803be8531abdc674dc631a28`; it changes documentation only and does not alter the runtime/test facts below.
- Inspected owners: `crates/neko-cli/src/main.rs`, `crates/neko-cli/tests/probe.rs`, exact-current `docs/CHATGPT_HANDOFF.md`, plus the current governance/spec/status/release documents required by `AGENTS.md`.
- Hosted cross-evidence on exact `206b4b9`: Rust CI run `35151540630` completed success; jobs `stable checks` and `nightly decode fuzz smoke` both succeeded. This is hosted cross-evidence only. The handoff separately records developer-local clean exact-tree provenance for `206b4b9`.

## Challenged invariant

The H-R9-028 initial stale/duplicate process regression must prove that exactly one authenticated semantic duplicate actually reaches the shared Carrier ACK owner and is classified accepted-empty, while producing no extra positive retirement, no rejection, unchanged Session logical-completion cardinality, and final Recovery zero.

The test must fail if the duplicate injection is absent, lost before the owner, rejected by crypto, or otherwise never reaches the accepted-empty branch.

## Finding — HIGH / evidence correctness

Exact `206b4b9` correctly adds one-shot server guards, so the stale/future injection seams fire at most once per bounded operation. The future fixture is discriminating because it requires exactly one typed `r9_udp_packet_ack_rejected`; absent/failing future injection would fail the test.

The stale fixture is not discriminating. The shared initial Carrier owner deliberately treats accepted-empty as a typed non-event: `applied=false`, `rejected=false`, with no diagnostic/counter. The final aggregate diagnostic exposes only `applied`, `rejected`, and `remaining_in_flight`.

The stale process test nevertheless labels a vector of `r9_udp_packet_ack_applied` lines as `empty` and asserts only:

- zero rejection;
- exactly two positive Carrier retirements for the two real reliable-owned sends;
- exactly two Session confirmations at offsets 0 and 16;
- final Recovery zero.

Those assertions are identical to the ordinary no-duplicate success shape. If the stale re-seal/send never happens, never reaches the client, or never reaches the Carrier owner, the test can still pass unchanged. Therefore it does not prove the claimed "exactly one accepted-empty classification" and H-R9-028 cannot be closed yet.

The runtime three-way classification itself remains accepted: `recv_udp_delivery_ack` preserves applied / accepted-empty / rejected, and accepted-empty is not mislabeled as a positive transition or a rejection. This finding is about missing process evidence, not a new protocol/state-machine defect.

## Smallest repair contract

Do not redesign ACK framing, Session semantics, Carrier architecture, crypto/wire architecture, deadlines, malformed budgets, or policy values. Keep the one-shot server guards.

Add a test-scoped diagnostic/counter at the existing initial shared Carrier owner (and settlement continuation if that owner can consume the duplicate there) that is emitted/incremented only for the existing `applied=false && rejected=false` Carrier outcome. It must be classification-only evidence and must not alter Recovery or Session state.

Then strengthen `reliable_udp_stale_ack_is_accepted_empty_not_rejected` to require exactly one accepted-empty classification, exactly two real positive Carrier retirements, zero typed rejection, unchanged exact Session confirmations for offsets 0/16, both processes success, and final Recovery zero. Prefer also requiring a server-side one-shot injection diagnostic if useful, but server send intent alone is not sufficient; the client owner must prove the accepted-empty classification.

Keep the future regression as a separate typed-rejection lane; do not weaken it while repairing stale proof.

## Exclusions

- No decoder/parser/crypto-framing semantics changed by this reviewer checkpoint.
- No new live/WAN question is created; `READY_LIVE: none` remains appropriate.
- No D019/capacity/signing/publication/release-authority decision is made.
- No release status flag is changed.

## Queue effect

Reopen H-R9-028 acceptance as H-R9-033 at the front of the local queue. After exact stale accepted-empty proof closes, continue the existing deep queue unchanged: positive P2 C1-C4 exact closure, post-return ACK arrival-order challenge, the two P4 single-domain suppression negatives, final R9-2 developer-local exact-tree provenance, R9-3 through R9-12, then dedicated independent cross-process R9 review and factual Q10/Q11/Q12 reconciliation.
