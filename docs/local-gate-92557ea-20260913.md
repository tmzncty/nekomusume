# Developer-local exact-tree provenance — `92557ea` repaired R1–R6 surface

Exact pushed source SHA: `92557ea1b0f158c88124da00a8a8af60dd8c0559` (`feat(reliable,carrier): authoritative final-frame-retirement signal (M-RUDP-006)`), the tree after the reviewer's second-round repairs Q1–Q3 closed.

Developer-local clean exact-tree gate. Hosted GitHub checks are separate cross-evidence; this is the developer-local anchor. Not a WAN/live claim, not an independent security/release approval.

## Commands run on a clean worktree of the exact SHA

```bash
PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh
git diff --check
git status --short
```

## Result

- UTC start `2026-09-13T12:10:57Z`, UTC end `2026-09-13T12:12:55Z`.
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`: exit 0.
- `git diff --check`: exit 0.
- `git status --short` before/after: empty.
- Host `Linux 6.8.0-137-generic x86_64 GNU/Linux`; `rustc 1.98.0 (88d9e12ae 2026-08-18)`.

## Repairs closed in this tree

- **H-RUDP-001A/B** (`c8193b7`): public health bridge is freshness-enforced — `health_sample()` is now private `raw_health_sample()`; `fresh_health_sample()` yields at most one sample per resolved `outcome_epoch` (advanced only by a resolved ACK-loss/PTO outcome, never a bare `on_sent`). Historical cumulative loss can no longer be replayed by sending more packets.
- **H-RUDP-005** (`4270f82`): `PacketAckTracker` now distinguishes `ack_eliciting`; `take_ack()` consumes a single pending obligation per new eligible packet — ACK-only packets cannot schedule an ACK-of-ACK, and polls emit nothing without new evidence.
- **M-RUDP-006** (`92557ea`): `Recovery::frame_outstanding`/`PathRecovery::frame_outstanding` give the authoritative final-copy retirement signal so `RetransmitBuffer` releases plaintext exactly once under overlapping copies.

`READY_LIVE: none` and all release flags are unchanged.
