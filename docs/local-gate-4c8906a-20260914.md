# Developer-local exact-tree provenance — `4c8906a` R9-2H demux repair

Exact pushed source SHA: `4c8906a717505485230a5ea31df768e005c546b9` (`fix(cli): persistent malformed budget across demux calls`).

Developer-local clean exact-tree gate. Not a WAN/live claim, not an independent security/release approval.

## Gate (clean detached worktree of exact SHA)

```bash
PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh   # exit 0
git diff --check                                  # exit 0
git status --short                                # empty before/after
```

- UTC start `2026-09-14T06:28:58Z`, UTC end `2026-09-14T06:30:56Z`.
- Host `Linux 6.8.0-137-generic x86_64 GNU/Linux`; `rustc 1.98.0`.

## Repairs (H-R9-006 + H-R9-007)

- `recv_udp_delivery_ack` now takes a persistent `malformed: &mut usize` —
  the invalid-input budget is owned by the whole reliable receive/settlement
  operation, so a Carrier ACK return (applied or rejected) can never reset it.
- The settlement phase drains Carrier packet ACKs through the same bounded
  demux owner; `r9_udp_packet_ack_outcomes` and `r9_udp_in_flight_settled`
  are emitted only after settlement, with `remaining_in_flight` authoritative.

`READY_LIVE: none` unchanged.
