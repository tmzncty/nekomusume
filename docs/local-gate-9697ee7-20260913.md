# Developer-local exact-tree provenance — `9697ee7` SessionRuntime terminal-cleanup repair

Exact pushed source SHA: `9697ee7a0045b39c6ef46c1951fefea486ccf13b` (`fix(session): symmetric terminal cleanup on idle-timeout and close-deadline`).

This records a developer-local clean exact-tree gate for the SessionRuntime terminal-cleanup repair. Hosted GitHub `stable checks`/`nightly decode fuzz smoke` are separate cross-evidence; this is the developer-local anchor. Not a WAN/live claim, not an independent security/release approval.

## Commands run on a clean worktree of the exact SHA

```bash
cargo test -p neko-session
PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh
git diff --check
git status --short   # final clean-tree confirmation
```

## Result

- UTC start `2026-09-13T01:45:12Z`, UTC end `2026-09-13T01:47:10Z`.
- `cargo test -p neko-session`: exit 0 (all runtime/ledger/datagram tests pass).
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`: exit 0.
- `git diff --check`: exit 0.
- `git status --short` before and after: empty (clean tree throughout).
- Host `Linux 6.8.0-137-generic x86_64 GNU/Linux`; `rustc 1.98.0 (88d9e12ae 2026-08-18)`; Cargo.lock unchanged.

No secrets, private endpoint topology, or absolute work paths are recorded. `READY_LIVE: none` and all release flags are unchanged by this provenance note.
