# Developer-local exact-tree provenance — `2dcb731` R9-2 multi-record repair

Exact pushed source SHA: `2dcb7316849a0ead04dfd6d4b96cd53480e085d2` (`fix(cli): R9-2A/B/C/D multi-record ownership + ACK demux + Data-admission + discriminating test`).

Developer-local clean exact-tree gate. Not a WAN/live claim, not an independent security/release approval.

## Gate (clean detached worktree of exact SHA)

```bash
PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh   # exit 0
git diff --check                                  # exit 0
git status --short                                # empty before/after
```

- UTC start `2026-09-13T22:56:44Z`, UTC end `2026-09-13T22:58:43Z`.
- Host `Linux 6.8.0-137-generic x86_64 GNU/Linux`; `rustc 1.98.0`.

## Repairs

- H-R9-002: reliable-owned records (`records[0]`, `records[1]`) no longer also
  take the legacy untracked `udp_uncertain_range_sent` path — the uncertain
  subset starts at `records[2]`; one Session range has exactly one Carrier
  owner.
- H-R9-003: `server_rt.on_packet_received` moved into the valid authenticated
  expected-Session Data path — non-Data/control/malformed packets never enter
  the outgoing R9 packet-ACK range.
- M-R9-004: process test pins multi-record once-delivery (`r9_udp_record_sent`
  offset:16) and asserts `records[1]` does not also traverse
  `udp_uncertain_range_sent`.

`READY_LIVE: none` unchanged.
