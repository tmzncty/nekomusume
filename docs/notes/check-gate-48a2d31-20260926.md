# Exact-tree check.sh provenance — 48a2d31 (2026-09-26)

Developer-local exact-tree gate on the final reachable pushed SHA for the
CarrierManager warm-candidate / UDP-failure / promotion / migrate-back pins.

- **SHA:** `48a2d3154581003f9d49a3b868813ee7c3092da8`
  (`test(carrier): pin CarrierManager warm-candidate, UDP-failure, promotion and migrate-back gates`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmMIG`) created from
  `git rev-parse origin/main` (asserted equal to local HEAD, to the literal
  pushed SHA, and to worktree HEAD); `git diff --check` exit 0;
  `git status --short` empty before and after
- **UTC start:** 2026-09-26T03:06:08Z (log `/tmp/nmmig-check2.log`)
- **UTC end:** 2026-09-26T03:12:16Z
- **Exit code:** 0 (host load average ~23.5 from unrelated jobs)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)

## Invalid first attempt (recorded, not evidence)

A first gate invocation (2026-09-26T02:59:17Z–03:05:26Z, EXIT=0, log
`/tmp/nmmig-check.log`) ran on `ac5c1d5`, the **parent** commit, not on
`48a2d31`. Cause: the developer issued commit, push and gate in **one
concurrent batch of tool calls** — the same process violation recorded in
`check-gate-a72a826-20260926.md` — so the gate resolved HEAD and
`origin/main` before the commit existed. Both were still `ac5c1d5`, so the
"origin/main == HEAD == worktree HEAD" assertion passed on the wrong tree. It
is evidence for neither SHA. The pre-commit workspace run (509/0) did finish
before that batch, so the "workspace green before commit" rule held; only
push → verify → gate ordering was broken. The rerun above was issued alone,
after fetch-verifying `origin/main` == `48a2d31`, and additionally asserts
the literal pushed SHA.

## Pre-commit sequence (strictly serial)

fmt --check 0, `clippy --workspace --all-targets -D warnings` 0, then
`cargo test --workspace` with no background mutation run active:
36 suites, 509 passed, 0 failed (first run). Commit, push and the first gate
were then batched concurrently (see above); the valid gate ran afterwards,
alone, once `origin/main` == HEAD == `48a2d31` was fetch-verified.

## Slice scope

Test-only, no semantics change. 48 mutants over `prepare_warm_candidate`,
`fail_warm_candidate`, `fail_udp_to_tcp`, `promote_warm_authenticated_resume`,
`promote_cold_authenticated_resume`, `promote_pending_target`,
`migrate_back_to_udp` and `set_active_{tcp,udp}` (the D064 readiness
accumulation itself stays excluded as warm/cold). 28 survived the full
neko-carrier package (16 suites, all complete). Nine new tests kill 20
(re-verified per mutant on the final tree). The remaining 8 are argued
equivalent in the commit message: W1/W2 (the two prepare guards are
redundant with each other), PW1b–PW4 (single-sided pending vs candidate
target/generation checks — a mismatched candidate is dropped at
`fail_udp_to_tcp`; dropping **both** sides of a pair is killed, verified as
PW_path_both / PW_gen_both), PP4 (hold already reset by `fail_udp_to_tcp`),
M11 (generation already required equal). No cross-crate re-run was done
because none of the 8 is argued killable.

## Evidence boundary

Developer-local execution only; not reviewer-local, hosted CI, WAN, or
performance evidence. No H-I4-119, D019, release-flag or policy-value change.
`READY_LIVE: none`.
