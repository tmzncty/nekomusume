# H-I4-107/108/109 exact-tree provenance

**Exact pushed source/test SHA:** `4c445ce36aa06edc1b3d4952036073865e3f20d0`
(`test(cli): H-I4-108 bounded_client_output timeout ≥ --duration + buffer`).

- **H-I4-107** (`75d65e6` + `10d97e7`): `bounded_wait_with_output`'s `kill()`
  `Err` and post-kill `try_wait` `Err` are explicit fail-closed panics —
  never a silent abandon with exit unproven. `bounded_client_output`/
  `multistream_client` bound the networked client `.output()` owners.
- **H-I4-108** (`4eadea9` + `4c445ce`): `run_client`/`authenticated`/
  `failover-client` owners spawn under `bounded_client_output` — piped
  pipes drained off-thread, `try_wait` to a local deadline, kill + bounded
  reap on timeout/error. Caller timeout is `--duration + 5s` so a healthy
  client is not false-positived.
- **H-I4-109** (`3bc1de3`): `bounded_wait_with_output` cleanup-ownership
  truthfulness — `kill()` `Err` and post-kill `try_wait` `Err` are explicit
  cleanup failures, never treated as exit/reap proof.

## Gate record

- worktree: clean `git worktree` at exact `4c445ce`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-23T03:02:29Z → UTC end 2026-09-23T03:08:20Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
