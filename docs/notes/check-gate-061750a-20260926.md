# Exact-tree check.sh provenance — 061750a (2026-09-26)

Developer-local exact-tree gate on the final reachable pushed SHA for the
neko-wire mutation-pin slice.

- **SHA:** `061750a59a7698a63969629585f1b443316fb963`
  (`test(wire): pin frame dispatch, record bounds, negotiation and ACK edges`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmWIRE3`) created from
  `git rev-parse origin/main` (asserted equal to local HEAD, to the literal
  pushed SHA, and to worktree HEAD); `git diff --check` exit 0;
  `git status --short` empty before and after
- **UTC start:** 2026-09-26T03:49:04Z (log `/tmp/nmwire-gate.log`, 1021 lines)
- **UTC end:** 2026-09-26T03:55:19Z
- **Exit code:** 0 (host load average ~33 from unrelated jobs)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)

An earlier invocation of the same unmodified recipe on the identical SHA and
clean tree also exited 0 (2026-09-26T03:42Z–03:48:57Z) but was not logged end
to end, so the logged run above is the recorded provenance run. Both are
same-tree gates on `061750a`; neither is a reviewer-local, hosted-CI, WAN or
performance result.

## Pre-commit sequence (strictly serial)

fmt --check 0, `clippy --workspace --all-targets -D warnings` 0, then
`cargo test --workspace` with no background mutation run active:
36 suites, 515 passed, 0 failed (03:33:44Z–03:37:46Z, load average ~24.6).
Commit, push, remote verification and the gate were four separate tool
invocations, in that order; `origin/main` was fetch-verified equal to HEAD
before the gate was issued.

## Slice scope

Test-only, +180 lines in `crates/neko-wire/src/lib.rs`, no semantics change.
65 hand-written mutants across the record codec, varint, frame
encode/decode/validation, the version negotiator, the ACK codec and the
packet header were probed with the whole neko-wire package (5 suites) as the
oracle: 59 killed, 6 equivalent. Six new tests kill 18 survivors; the
equivalence reasons for the other 6 are in the commit message.

Equivalence evidence for the 6 survivors came from a standalone differential
harness kept outside the repository (canonical vs mutated copies, 300275
comparisons: every varint of length <= 4 over a six-byte alphabet, every
length 5..=13 pattern over {0x80, 0x01}, every record header shape,
every hello-shaped input of length 4..=8, every (frame type byte, length
0..=1100) pair). It reported zero divergences for V_take11, V_checked_equiv,
D_payload_decl and N_ord_ge, and it refuted the first reading of F_path8_ge
(`len == 8` -> `len >= 8`, 4376 witnesses where the length rule is applied
before the truncation check), which is therefore pinned by a regression
rather than written off. N_client_store and A_map_fallback are equivalent by
construction (decode_response pins every accepted response byte;
decode_varint only ever returns the three explicitly mapped variants).

## Evidence boundary

Developer-local execution only; not reviewer-local, hosted CI, WAN, or
performance evidence. No production source changed. No H-I4-119, D019,
release-flag or policy-value change. `READY_LIVE: none`.
