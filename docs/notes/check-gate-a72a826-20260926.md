# Exact-tree check.sh provenance — a72a826 (2026-09-26)

Developer-local exact-tree gate on the final reachable pushed SHA for the
CarrierHealth limit/badness/recovery pins.

- **SHA:** `a72a826d8d4251c3f25b969c132d0c635692756f`
  (`test(carrier): pin CarrierHealth limit validation, badness thresholds and recovery`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmHLT`) created from
  `git rev-parse origin/main` (asserted equal to local HEAD and worktree HEAD);
  `git diff --check` exit 0; `git status --short` empty before and after
- **UTC start:** 2026-09-26T00:38:57Z (log `/tmp/nmhlt-check2.log`)
- **UTC end:** 2026-09-26T00:45:06Z
- **Exit code:** 0 (only gate run on this SHA; host load average ~25 from unrelated jobs)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)

## Process deviation (recorded, not evidence)

The developer issued the pre-commit workspace test run, the commit/push, and
a gate invocation **concurrently** instead of strictly serially:

1. The pre-commit `cargo test --workspace` ran concurrently with a gate and
   stopped at 19 suites with 1 failure; the commit/push did **not** wait for
   it. This violates the "full workspace green before commit" rule.
2. That concurrent gate resolved `origin/main` before the push landed, so it
   ran on the **parent** `ee6dce1` (2026-09-26T00:30:10Z–00:34:20Z, EXIT=101):
   one `neko-cli --test probe` failure,
   `authenticated_tcp_benchmark_echoes_exact_payload_and_hash` (loopback
   readiness under load ~25). It is not evidence for either SHA.

Remediation, strictly serial afterwards: `cargo test --workspace` at
`a72a826` — 36 suites, 489 passed, 0 failed; then the single clean gate
above. The slice touches only `neko-carrier` unit tests; the probe failure is
the known load-sensitive loopback-readiness class (see
`check-gate-aedb7d3-20260926.md`).

## Slice scope

Test-only, no semantics change. Mutation probing on `ee6dce1` of
`CarrierHealth` / `CarrierHealthEvidence` (19 mutants) found 6 survivors
under the neko-carrier lib tests; all 6 also survived the rest of the
workspace suite (spot-checked manually for the pto boundary mutant: 20 suites,
333 passed, 0 failed with the mutant applied). Three new tests kill all 6
(re-verified per mutant): `degrade_after`/`recover_after`/`max_paths` zero
rejected and `fail_after == degrade_after` accepted; measured-sample badness
inclusive at `pto >= 3` and `loss_per_mille >= 500` (same boundary as
`CarrierManager::score` healthy); plus known-path observation at exactly
`max_paths`, bad-streak reset by a good sample, exact `recover_after`
recovery from Degraded, and `CarrierHealthEvidence` rejecting
`max_samples == 0`. No equivalent mutants.

## Evidence boundary

Developer-local execution only; not reviewer-local, hosted CI, WAN, or
performance evidence. No H-I4-119, D019, release-flag or policy-value change.
`READY_LIVE: none`.
