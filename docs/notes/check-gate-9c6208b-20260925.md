# Exact-tree check.sh provenance — 9c6208b (2026-09-25)

Developer-local exact-tree gate on the final reachable pushed SHA for the
complete swap-atomicity fix in FaultInjectCarrier (first-half restore +
duplicate exactly-two under failure).

- **SHA:** `9c6208b236824d50a3aa9be2f0e2d8ed63c9dfa2`
  (`fix(carrier): complete swap atomicity — first-half restore + duplicate
  exactly-two under failure`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmAA`) at the pushed SHA,
  `git status --short` empty before and after the run; `git diff --check`
  exit 0
- **UTC start:** 2026-09-25T10:09:11Z (log captured in `/tmp/nmaa-check.log`)
- **UTC end:** 2026-09-25T10:15:17Z
- **Exit code:** 0
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` stable
  (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)
- **Scope:** full `scripts/check.sh` — `cargo fmt --check`, `cargo check
  --workspace --locked`, `cargo test --workspace --locked` (36 "test result:
  ok" suites; neko-carrier lib 104/104 including the two new regressions
  `reorder_swap_first_half_failure_restores_pending` and
  `duplicate_partial_delivery_never_exceeds_two_copies`), `cargo clippy
  --workspace --all-targets --locked -- -D warnings`, plus all
  governance/status/evidence/preauth/shell/release/reproducibility/smoke
  policy scripts and bounded warm-failover adapter tests. All passed.
- **Slice scope:** fixture-repair (test-support code only, no production
  owner). Two residual gaps in the reorder swap error paths found by
  self-review of c389ec6: (1) first-half failure previously dropped the
  taken-out withheld record silently — now always restored (zero copies of
  it delivered, no multiplicity risk); (2) duplicate mode's partial
  second-half delivery (one copy in flight) previously restored
  unconditionally, which a retry would turn into three total copies —
  `deliver_copies` now tracks admitted copies: zero-copy failure restores
  cleanly; one-copy partial failure leaves the slot empty with the record
  under-delivered but in flight and the error surfaced. Exactly-two
  multiplicity is preserved under all failure combinations.
- **Note:** one test-arithmetic amend (398d959 → 9c6208b: the
  first-half-failure regression needed the queue pre-filled by a prior
  successful swap; the initial version's queue was empty so the first half
  could not fail) preceded this run; the 398d959 tree failed its local
  full-lib run and was never gated.
