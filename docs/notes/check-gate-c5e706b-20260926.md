# Exact-tree check.sh provenance — c5e706b (2026-09-26)

Developer-local exact-tree gate on the final reachable pushed SHA for the
neko-carrier fault-injector mutation-pin slice.

- **SHA:** `c5e706b7e6d8ea4f22590f3114da12d4c3ea3e45`
  (`test(carrier): pin fault-injector loss stream, multiplicity and delegation`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmFIG`) created from
  `git rev-parse origin/main` (asserted equal to local HEAD, to the literal
  pushed SHA, and to worktree HEAD); `git diff --check` exit 0;
  `git status --short` empty before and after
- **UTC start:** 2026-09-26T05:43:39Z (log `/tmp/nmfig-gate.log`)
- **UTC end:** 2026-09-26T05:49:46Z
- **Exit code:** 0 (host load average ~19.3 from unrelated jobs)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)

## Pre-commit sequence (strictly serial)

fmt --check 0, `clippy --workspace --all-targets -D warnings` 0, then
`cargo test --workspace`: 36 suites, 537 passed, 0 failed
(05:35:15Z-05:43:14Z, load average ~16), no re-run needed. Commit, push,
remote verification and the gate were four separate tool invocations;
`origin/main` was fetch-verified equal to HEAD before the gate was issued.

## Slice scope

Test-only, +124 lines in `crates/neko-carrier/src/lib.rs`
(`mod fault_inject_boundary_tests`), no semantics change. 24 hand-written
mutants over FaultInjectCarrier::new, draw, blocked, pending_len, the send
drop-class chain (close_after, post-close, blackhole, one_way,
max_payload_bytes, loss_window, loss_percent, delay), the duplicate/reorder
delivery paths, close and recv were probed with the full neko-carrier
package (16 suites, 187 tests) as the oracle: 21 killed, 3 equivalent. The
two new tests kill 4 survivors; the equivalence reasons for the other three
are in the commit message.

The exact surviving-send-index sets (seed 0 at 50% and 3% loss), the reorder
sequence and the loss-window indices were measured first in a throwaway
probe module inside the scratch worktree (`/tmp/nmFI`, not committed). One
witness was corrected there: a memory pair cannot receive on the side that
sent, so the delegation case sends from the raw endpoint and receives
through the wrapper.

## Evidence boundary

Developer-local execution only; not reviewer-local, hosted CI, WAN, or
performance evidence. No production source changed. No H-I4-119, D019,
release-flag or policy-value change. `READY_LIVE: none`.
