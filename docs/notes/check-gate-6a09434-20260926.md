# Exact-tree check.sh provenance — 6a09434 (2026-09-26)

Developer-local exact-tree gate on the final reachable pushed SHA for the
neko-cli pre-auth admission boundary-pin slice.

- **SHA:** `6a09434501bd70d5034776c6276495ba135a92bc`
  (`test(cli): pin pre-auth admission identity, write budgets and response deadline seams`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmCLG`) created from the
  literal pushed SHA `6a09434` (worktree head asserted equal to it);
  `git diff --check` exit 0; `git status --short` empty before and after
- **UTC start:** 2026-09-26T07:07:00Z (log `/tmp/nm-cli-gate.log`, 1062 lines)
- **UTC end:** 2026-09-26T07:13:06Z
- **Exit code:** 0 (host load average ~14.3 from unrelated jobs)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)`

## Pre-commit sequence (strictly serial)

`cargo fmt --all` / `--check` 0, `clippy --workspace --all-targets -D warnings`
0, then `cargo test --workspace`: 36 suites, 557 passed, 0 failed
(07:02:29Z-07:06:29Z, load average ~14). Commit, push, remote verification and
the gate were separate tool invocations; `origin/main` was fetch-verified equal
to HEAD before the gate was issued. The gate was issued **alone** — no
concurrent batch — and asserted the literal SHA rather than a moving
`origin/main`, which are the two failures recorded in the previous round's
`check-gate-eb81ae0-20260926.md`.

## Slice scope

Test-only, +252 lines in `crates/neko-cli/src/preauth.rs` (eight tests added
inside the existing `mod tests`), no semantics change.

Oracle note: `neko-cli` has no lib target, so its unit tests live in the binary
and the screen command is `cargo test -q -p neko-cli --bin neko-cli` (~1s
incremental, ~0.4s run). The 235s `tests/probe.rs` integration suite is not in
the loop. During recon `cargo test -p neko-cli` (which includes probe.rs)
reported 1 failure of 74 under host load ~14 and passed 74/74 on an immediate
clean re-run; that suite spawns real server processes and loopback sockets, so
a load-induced flake there is the known, expected condition rather than a new
defect.

31 mutants were hand-written. One is invalid on my side (`checked_sub` ->
`saturating_sub` in `remaining_budget` does not compile, since `saturating_sub`
returns `u64` and the surrounding code expects an `Option`); it is recorded as
an invalid construction, not as an equivalence. Of the remaining 30: **24
killed, 6 equivalent**. The eight new tests kill 15 of the 24; eight were
already killed by the pre-existing suite. Equivalence reasons for all six are
in the commit message and are backed by reachable source in `neko-crypto`
(notably: the outer `abandon_input_record` rejects the state itself at
lib.rs:1417; `dequeue` fails on the missing state at 1518-1521;
`expire_states` can only fail for a backwards clock at 1692-1695;
`charge_input`/`charge_response` reject the state on any error at 1428-1432 and
1538-1542, terminalizing the ticket so the removed rollbacks cannot be
observed; and `complete_response_checked` only clears `pending_response` at
1669-1672, whose sole differing reader is unreachable because a second response
attempt for the same state is already rejected canonically - measured, not
assumed).

One witness was rewritten: the elapsed-permit test originally forged the permit
with a backdated clock (`now = 0`), which is rejected as a backwards clock
before any deadline logic, so the final test uses the real flow plus a ~200ms
sleep against the 100ms default send budget. A test I wrote asserting that a
suppressed response frees the state **failed against unmutated code**; it was
removed rather than weakened, and that failure is the evidence for the
`SUP_complete_off` equivalence above.

## Evidence boundary

Developer-local execution only; not reviewer-local, hosted CI, WAN, or
performance evidence. No production source changed. No H-I4-119, D019,
release-flag or policy-value change. `READY_LIVE: none`.
