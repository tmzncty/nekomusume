# Exact-tree check.sh provenance — 68de291 (2026-09-26)

Developer-local exact-tree gate on the final reachable pushed SHA for the
neko-session runtime / process-message mutation-pin slice.

- **SHA:** `68de291f91100f046a7e761486b0534de8d264de`
  (`test(session): pin runtime limit validation, exact deadlines and process-message framing`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmRTG`) created from
  `git rev-parse origin/main` (asserted equal to local HEAD, to the literal
  pushed SHA, and to worktree HEAD); `git diff --check` exit 0;
  `git status --short` empty before and after
- **UTC start:** 2026-09-26T04:30:30Z (log `/tmp/nmrt-gate.log`)
- **UTC end:** 2026-09-26T04:36:55Z
- **Exit code:** 0 (host load average ~32.5 from unrelated jobs)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)

## Pre-commit sequence (strictly serial)

fmt --check 0, `clippy --workspace --all-targets -D warnings` 0, then
`cargo test --workspace`. The first workspace run on this tree
(04:21:56Z-04:29:56Z, load average ~33.6) reported one failure:
`neko-cli` tests/probe.rs
`sigterm_after_ready_stops_and_releases_tcp_and_udp_bindings` - the known
load-induced loopback flake, in a crate this slice does not touch
(neko-session test modules only). One clean re-run of the identical tree,
issued alone with no background job, passed 36 suites 526/0. Commit, push,
remote verification and the gate were then four separate tool
invocations; `origin/main` was fetch-verified equal to HEAD before the
gate was issued.

## Slice scope

Test-only, +346 lines in `crates/neko-session/src/lib.rs`, no semantics
change. 61 hand-written mutants over SessionRuntime::new limit validation,
open_stream/close_stream/stream_state, queue_send, pop_send, receive,
delivery_ack, tick/check/close_graceful/cancel/clear_runtime_state,
ProcessMessage encode/decode and DatagramRuntime were probed with the
whole neko-session package (3 suites) as the oracle: 53 killed, 8
equivalent. Eight new tests kill 22 survivors; the equivalence reasons for
the other 8 are in the commit message.

The canonical outcomes asserted by the new tests were confirmed first in a
throwaway probe module inside the scratch worktree (`/tmp/nmRT`, not
committed); that probe is also what corrected the trailing-byte witness
(a byte added to a frame already at the 4096-byte limit is caught by the
frame bound, so the exact-length rule is pinned with a 32-byte frame
instead). Two mutants first read as gaps - T_check_idle_ok and
M_hello_len0_off - were re-classified by the probe: the first is
equivalent (tick already returns the idle error for the same condition),
the second was pinned with the corrected witness.

## Evidence boundary

Developer-local execution only; not reviewer-local, hosted CI, WAN, or
performance evidence. No production source changed. No H-I4-119, D019,
release-flag or policy-value change. `READY_LIVE: none`.
