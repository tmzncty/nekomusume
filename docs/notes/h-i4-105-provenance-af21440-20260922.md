# H-I4-105 exact-tree provenance

**Exact pushed source/test SHA:** `af21440` (`test(cli): H-I4-105 bounded
auxiliary peer ownership`).

Auxiliary peer threads that the test later `join()`s now have independent
test-local completion bounds: `bounded_accept` (nonblocking + poll) for TCP
listeners, `set_read_timeout` before the first `recv_from` for UDP peers,
and `set_read_timeout` on accepted streams before frame reads. A CLI
regression that never connects/sends can no longer strand the join — it
becomes deterministic negative evidence instead of a hung gate.

Owners: `matrix_probe` TCP/UDP peers,
`tcp_and_udp_transcript_mismatch_rejects_before_application_echo` peers,
`tcp_and_udp_reject_unsupported_selected_version_before_noise` TCP peer, and
`multistream.rs` `executable_rejects_one_byte_different_negotiation_binding`
TCP peer.

## Gate record

- worktree: clean `git worktree` at exact `af21440`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-22T20:02:53Z → UTC end 2026-09-22T20:08:40Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
