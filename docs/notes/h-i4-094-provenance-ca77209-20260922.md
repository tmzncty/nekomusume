# H-I4-094 exact-tree provenance

**Exact pushed source/test SHA:** `ca772093f4fc46bda3be9aade00d4fc884b48797`
(`test(cli): H-I4-094 ReadyProof — baseline consumes ready-barrier proof`).

`ReadyServer` now carries `ReadyProof` — a private-constructor value
obtainable only after the server diagnostic `start` has been observed by
`ready_failover_server` / `ready_endpoint_rebind_server` / `start_server_for`.
The pre-churn `gated_resource_snapshot` destructures it inside the gate, so a
refactor moving the baseline above the ready barrier fails to compile. The
post edge remains barrier-derived via `BarrierProof`.

## Gate record

- worktree: clean `git worktree` at exact `ca77209`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-22T04:50:53Z → UTC end 2026-09-22T04:56:50Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
