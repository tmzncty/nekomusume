# Exact-tree check.sh provenance — 75c7be4 (2026-09-26)

Developer-local exact-tree gate on the final reachable pushed SHA for the
CarrierManager limit/eligibility/hold/margin pins.

- **SHA:** `75c7be4053cbf69775c6963faa58d254dd42af7e`
  (`test(carrier): pin CarrierManager limits, eligibility, hold accumulation and inclusive margin`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmMGR`) created from
  `git rev-parse origin/main` (asserted equal to local HEAD and worktree HEAD);
  `git diff --check` exit 0; `git status --short` empty before and after
- **UTC start:** 2026-09-26T02:02:42Z (log `/tmp/nmmgr-check.log`)
- **UTC end:** 2026-09-26T02:08:52Z
- **Exit code:** 0 (only gate run on this SHA; host load average ~25 from unrelated jobs)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)

## Pre-commit sequence (strictly serial)

fmt --check 0 and `clippy --workspace --all-targets -D warnings` 0, then
`cargo test --workspace`. The first workspace run (01:29Z, uncommitted tree)
had one `neko-cli --test probe` failure,
`authenticated_tcp_benchmark_echoes_exact_payload_and_hash` (probe.rs:118,
loopback readiness), while a background mutation run was also compiling and
testing the workspace (load ~24). After that background run finished, one
clean re-run: 36 suites, 494 passed, 0 failed. Only then commit → push →
fetch (`origin/main` == HEAD) → this gate.

## Slice scope

Test-only, no semantics change. Mutation probing of `CarrierManager`
(`new`/`observe`/`score`/`choose`/`set_active_tcp`, 25 mutants) left 12
survivors under the neko-carrier lib tests. 11 were re-run against the rest
of the workspace (`--workspace --exclude neko-carrier`; every run took
~240 s and executed all 20 suites / 333 tests, so none is a truncated false
survivor): 10 still survive, only "active best path does not reset hold" is
caught elsewhere. (C3, the `switch_margin <= 0` mutant, was already killed.)
Five new tests kill all 25 (re-verified per mutant on the final tree):

- `min_hold_events == 0` / `max_paths == 0` rejected; exactly `max_paths`
  distinct paths fit; `set_active_tcp` refuses an unobserved path
- `choose()` never selects an unhealthy path even if it scores higher
- hold accumulates through `choose()` itself — the existing
  `carrier_scoring_and_hysteresis_bound_oscillation` sets `m.hold = 3`
  directly, so a `choose()` that never increments hold survived it —
  the switch happens on the `min_hold_events`-th held call, the margin is
  inclusive (`>=`), and a switch resets hold
- an improvement smaller than the margin never switches; the active path
  becoming best again resets hold
- an active path with no sample yields to the best healthy path after hold

No equivalent mutants.

## Correction to check-gate-a72a826-20260926.md

That note (and commit a72a826's message) stated all 6 CarrierHealth
survivors "also survived the rest of the workspace suite", but only 1 of 6
(the `pto > 3` mutant) had actually been verified; the background re-run
reported SURVIVED in seconds and was not trustworthy. Re-verified this round
on `ad17d89` with a timed probe: the other 5 (`degrade_after == 0` dropped,
`fail_after <= degrade_after`, `recover_after == 0` dropped,
`max_paths == 0` dropped, `pto >= 4`) each ran ~240 s, 20 suites /
333 tests, rc 0 — so the claim holds for all 6, now with evidence.

## Evidence boundary

Developer-local execution only; not reviewer-local, hosted CI, WAN, or
performance evidence. No H-I4-119, D019, release-flag or policy-value change.
`READY_LIVE: none`.
