# Exact-tree check.sh provenance — 4511e4f (2026-09-24)

Developer-local exact-tree gate on the final reachable pushed SHA after the
H-I4-120 governance rollback + semantics-neutral test + erratum. Supersedes
the incomplete `f2f33b2` record (approximate UTC end, missing Rust version).

- **SHA:** `4511e4f147eb9511907bb4ec52cad687f113bb7a`
  (`test(reliable): H-I4-120 complete — semantics-neutral test + erratum SHA fix`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree, `git status --short` empty;
  `git diff --check` exit 0
- **UTC start:** 2026-09-24T12:06:00Z (session start; log captured in-session)
- **UTC end:** 2026-09-24T12:11:00Z (session idle; `exit 0`)
- **Exit code:** 0
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` stable
- **Scope:** full `scripts/check.sh` — `cargo fmt --check`, `cargo check`,
  `cargo test --workspace`, `cargo clippy -D warnings`, plus all
  governance/status/evidence/preauth/shell/release/reproducibility/smoke
  policy scripts and bounded live warm-failover adapter tests. All passed.
- **Note:** an earlier detached run on `f2f33b2` was killed before writing
  `EXIT=`; the single observed `authenticated_tcp_benchmark_echoes_exact_payload_and_hash`
  FAILED was a socket-timeout flake — the rerun on this exact SHA passed with
  exit 0.
