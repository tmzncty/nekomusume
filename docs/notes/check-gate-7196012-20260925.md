# Exact-tree check.sh provenance — 7196012 (2026-09-25)

Developer-local exact-tree gate on the final reachable pushed SHA for the
R-MBOX grouped release/item-4 factual reconciliation (documentation-only
slice: status.md evidence-boundary refresh + Era-4 ledger track M basis
refresh).

- **SHA:** `71960127ced82856390a186a36771bc38817f956`
  (`docs(status): R-MBOX grouped release/item-4 factual reconciliation`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmGR`) at the pushed SHA,
  `git status --short` empty before and after the run; `git diff --check`
  exit 0
- **UTC start:** 2026-09-25T04:02:14Z (log captured in
  `/tmp/neko-7196012-check.log`)
- **UTC end:** 2026-09-25T04:08:20Z
- **Exit code:** 0
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` stable
  (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)
- **Scope:** full `scripts/check.sh` — `cargo fmt --check`, `cargo check
  --workspace --locked`, `cargo test --workspace --locked` (36 "test result:
  ok" suites; neko-carrier lib 96/96), `cargo clippy --workspace
  --all-targets --locked -- -D warnings`, plus all
  governance/status/evidence/preauth/shell/release/reproducibility/smoke
  policy scripts (including `check-era4-closure.py` over the refreshed
  ledger) and bounded warm-failover adapter tests. All passed.
- **Slice scope:** documentation-only factual reconciliation. `docs/status.md`
  gained a dated R-MBOX fixture reconciliation section (exact primitive
  inventory, gate SHAs, precise evidence boundary, untouched H-I4-119/D019
  seams); `docs/era4-ledger-2026-08-30.json` track M
  `classification_basis` refreshed with the new fixture capabilities and
  SHAs. Track M classification value unchanged
  (`ALREADY_SUFFICIENT_FOR_BOUNDED_QUESTION`); no policy, no release flag,
  no other ledger row modified.
