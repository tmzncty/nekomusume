# Exact-tree check.sh provenance — 523888d (2026-09-25)

Developer-local exact-tree gate on the final reachable pushed SHA for the
RTT update / PTO arithmetic branch pins.

- **SHA:** `523888d968ce87a113e4418fe150ad04ae57e578`
  (`test(reliable): pin RTT update branches and PTO floor/base/backoff cap`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmRU`) at the pushed SHA;
  `git status --short` empty before and after the run; `git diff --check` exit 0
- **UTC start:** 2026-09-25T14:42:46Z (log captured in `/tmp/nmru-check.log`)
- **UTC end:** 2026-09-25T14:48:51Z
- **Exit code:** 0
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)
- **Scope:** full `scripts/check.sh`. Pre-commit local workspace run: 36
  suites, 462 tests passed, 0 failed; neko-reliable lib 40/40 including the
  two new tests.
- **Slice scope:** test-only, no semantics change. Mutation probing of
  `RttEstimator::update` / `pto_us` on `96185c8` (full workspace test
  suite per mutant) found seven surviving mutants: ack_delay adjustment
  applied unconditionally; adjustment guard made strict (`>`); `min_us` not
  tracked; zero samples accepted; PTO granularity floor removed; uninitialized
  1 s PTO base changed; backoff exponent cap lowered 63→62. Four other mutants
  were already killed. New tests
  `rtt_update_ack_delay_min_and_zero_sample_branches` and
  `pto_granularity_floor_uninitialized_base_and_backoff_cap` kill all seven
  (re-verified per mutant). `unwrap_or(u64::MAX)` after
  `checked_shl(pto_count.min(63))` is unreachable and remains an equivalent
  mutant by construction.
- **Evidence boundary:** developer-local execution only; not reviewer-local,
  hosted CI, WAN, or performance evidence. Does not touch H-I4-119 (PTO reset
  semantics gate), D019, release flags or policy values. `READY_LIVE: none`.
