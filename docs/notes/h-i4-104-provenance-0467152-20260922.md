# H-I4-104 exact-tree provenance

**Exact pushed source/test SHA:** `0467152d4e595c30ba6373333cd19bf8fa382cc0`
(`test(cli): H-I4-104 start_periodic_server bounded readiness`).

`start_periodic_server` replaced its raw `read_line` readiness loop with the
shared `wait_for_ready_marker(5s)` primitive — the bounded wait converges
child/pipe ownership on timeout/EOF/error. The
`start_periodic_server_bounded_when_binary_exits_silently` negative
regression proves bounded panic on a silently-exiting binary, not a hang in
`read_line`.

## Gate record

- worktree: clean `git worktree` at exact `0467152`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-22T17:50:09Z → UTC end 2026-09-22T17:56:00Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
