# Exact-tree check.sh provenance — 21c1a61 (2026-09-25)

Developer-local exact-tree gate on the final reachable pushed SHA for
R-MBOX-REORDER-DELAY (bounded reorder + duplicate implementation in
`FaultInjectCarrier`).

- **SHA:** `21c1a61cb4041b6874d58de8d97a44ffea89fb5c`
  (`test(carrier): R-MBOX-REORDER-DELAY — implement bounded reorder +
  duplicate in FaultInjectCarrier`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmRD1`) at the pushed SHA,
  `git status --short` empty before and after the run; `git diff --check`
  exit 0
- **UTC start:** 2026-09-24T19:45:43Z (log captured in
  `/tmp/neko-21c1a61-check.log`)
- **UTC end:** 2026-09-24T19:51:48Z
- **Exit code:** 0
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` stable
  (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)
- **Scope:** full `scripts/check.sh` — `cargo fmt --check`, `cargo check
  --workspace --locked`, `cargo test --workspace --locked` (36 "test result:
  ok" suites; neko-carrier lib 91/91 including the five new
  deterministic reorder/duplicate regressions:
  `reorder_swaps_adjacent_pairs_and_is_bounded`,
  `duplicate_emits_exact_multiplicity_and_ordering`,
  `reorder_with_loss_keeps_buffer_bounded_and_deterministic`,
  `duplicate_reorder_combined_multiplicity_and_ordering`,
  `delay_ms_lower_bound_is_bounded_and_configured`), `cargo clippy
  --workspace --all-targets --locked -- -D warnings`, plus all
  governance/status/evidence/preauth/shell/release/reproducibility/smoke
  policy scripts and bounded warm-failover adapter tests. All passed.
- **Semantics implemented (fixture-only, bounded):** `reorder` = bounded
  one-record pending slot; a surviving record is withheld and released
  after the NEXT surviving record (adjacent-pair swap); dropped records
  never enter the buffer and never release a withheld record. `duplicate`
  = exactly two consecutive copies per surviving record at the delivery
  point. `delay_ms` = pre-existing send-time sleep, now covered by a
  lower-bound-only test (2×2ms ≥ 4ms) with no wall-clock upper bound. No
  production timer/readiness/hysteresis/congestion/capacity policy
  invented; existing loss/blackhole/close/one_way behavior unchanged.
- **Note:** one formatting-only amend (02c0b67 → 21c1a61) preceded this
  run; the 02c0b67 gate attempt failed at `cargo fmt --check` before any
  test executed.
