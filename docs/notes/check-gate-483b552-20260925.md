# Exact-tree check.sh provenance — 483b552 (2026-09-25)

Developer-local exact-tree gate on the final reachable pushed SHA for
R-MBOX-RESOURCE (repeated-transition resource convergence regression).

- **SHA:** `483b55297d342f373616f46631639c07d0536d55`
  (`test(carrier): R-MBOX-RESOURCE — repeated-transition resource
  convergence`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmRES`) at the pushed SHA,
  `git status --short` empty before and after the run; `git diff --check`
  exit 0
- **UTC start:** 2026-09-25T04:50:53Z (log captured in
  `/tmp/neko-483b552-check.log`)
- **UTC end:** 2026-09-25T04:56:59Z
- **Exit code:** 0
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` stable
  (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)
- **Scope:** full `scripts/check.sh` — `cargo fmt --check`, `cargo check
  --workspace --locked`, `cargo test --workspace --locked` (36 "test result:
  ok" suites; neko-carrier lib 98/98 including the new
  `repeated_transitions_converge_with_no_leaked_in_flight_state`), `cargo
  clippy --workspace --all-targets --locked -- -D warnings`, plus all
  governance/status/evidence/preauth/shell/release/reproducibility/smoke
  policy scripts and bounded warm-failover adapter tests. All passed.
- **Semantics pinned (fixture-only):** eight bounded UDP→TCP→UDP cycles each
  carrying a REAL unconfirmed in-flight record across the transition; after
  every cycle the retained ownership returns to the empty baseline
  (uncertain set drains via authoritative replay+confirm; pending_switch
  clears; replayed already-delivered records are deduped Ok(false)); the
  same small fixed capacities (16 entries / 64 bytes) suffice for all eight
  cycles — absence of monotonic leaked in-flight state, not capacity
  pressure. Counters exactly cycle-counted (fc.switches=8; m.switches=16;
  cold_recoveries=8; rx duplicate_bytes=16). No new limits invented;
  H-I4-119/D019 untouched.
