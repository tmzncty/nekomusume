# Exact-tree check.sh provenance — 056366e (2026-09-25)

Developer-local exact-tree gate on the final reachable pushed SHA for
R-MBOX-REPEATED-FAILOVER (bounded repeated failover/recovery cycle
regression).

- **SHA:** `056366eb6a14e5065f3d1fb3902bc1f757e2bd98`
  (`test(carrier): R-MBOX-REPEATED-FAILOVER — bounded repeated failover/
  recovery cycles`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmRF`) at the pushed SHA,
  `git status --short` empty before and after the run; `git diff --check`
  exit 0
- **UTC start:** 2026-09-25T04:22:08Z (log captured in
  `/tmp/neko-056366e-check.log`)
- **UTC end:** 2026-09-25T04:28:14Z
- **Exit code:** 0
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` stable
  (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)
- **Scope:** full `scripts/check.sh` — `cargo fmt --check`, `cargo check
  --workspace --locked`, `cargo test --workspace --locked` (36 "test result:
  ok" suites; neko-carrier lib 97/97 including the new
  `repeated_failover_cycles_keep_single_active_and_no_duplicates`),
  `cargo clippy --workspace --all-targets --locked -- -D warnings`, plus all
  governance/status/evidence/preauth/shell/release/reproducibility/smoke
  policy scripts and bounded warm-failover adapter tests. All passed.
- **Semantics pinned (fixture-only):** three bounded UDP→TCP→UDP cycles over
  one in-memory pair driven ONLY by existing semantics
  (`fail_udp_to_tcp` / `promote_cold_authenticated_resume` /
  `migrate_back_to_udp` / `udp_pto_at` / `udp_progress` / `tcp_resend` /
  `confirm` / `receive`). Asserted: single active owner at every step,
  strictly monotonic generations (+1 per cycle), the real hold gate
  exercised (first migration-back attempt returns HoldGate state-unchanged;
  second commits), PTO-window reset via `udp_progress` making cycles
  independent, exactly-counted metrics (controller switches=3; manager
  switches=6 = 3 promoted failovers + 3 committed migration-backs;
  cold_recoveries=3), zero duplicate Session delivery. No new policy
  invented; H-I4-119/D019 untouched.
- **Note:** one clippy-only amend (08d13ea → 056366e: unused-mut +
  explicit-counter-loop) preceded this run; the 08d13ea gate attempt failed
  at clippy before the full test phase completed.
