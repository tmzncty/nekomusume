# H-I4-117 exact-tree provenance

**Exact pushed source/test SHA:** `2ba5b960ebb13f1bfe6c5e4b07438385f5aab673`
(`test(carrier): H-I4-117 orchestrate test tolerates post-collapse send refusal`).

`ReliableUdpRuntime::pto_probe` now applies `PathRecovery::persistent_congestion()`
after a legitimate non-empty-runtime PTO increments the count — PTO 3 collapses
`cwnd` to `2 * MSS`. `runtime_pto_persistent_congestion_collapses_window_at_
threshold` proves below-threshold (PTO 1/2) unchanged and threshold (PTO 3)
collapse. `pacing_and_send_decision` now asserts executable persistent-congestion
evidence (below-threshold `can_send` unchanged, threshold `can_send(1601)`
refused). `reliable_udp_runtime_orchestrates` tolerates post-collapse
`CongestionWindowFull` on late sends while continuing PTO/health polling.

## Gate record

- worktree: clean `git worktree` at exact `2ba5b96`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-24T01:54:02Z → UTC end 2026-09-24T01:59:50Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
