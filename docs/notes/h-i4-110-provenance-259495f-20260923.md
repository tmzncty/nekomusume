# H-I4-110 exact-tree provenance

**Exact pushed source/test SHA:** `259495fad9949a526b12187ba59cf74aec0ee7f7`
(`test(cli): H-I4-110 canonical failover --role client bounded`).

`executable_loopback_controlled_udp_stop_tcp_resume`'s canonical
`failover --role client` now runs under `bounded_client_output` — a live
client can no longer strand `finish_server`. Caller timeout is
`--duration 3 + 5s = 8s`.

## Gate record

- worktree: clean `git worktree` at exact `259495f`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-23T03:49:56Z → UTC end 2026-09-23T03:55:40Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
