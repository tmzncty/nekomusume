# H-I4-100 exact-tree provenance

**Exact pushed source/test SHA:** `833962e` (`test(cli): H-I4-100
bounded_wait_exit — direct wait() replaced`).

`bounded_wait_exit(child, deadline)` polls `try_wait` against a
caller-supplied deadline; on deadline the child is still unproven-exited, so
ownership routes through `bounded_reap_or_kill` (now `&mut Child`) and the
caller's contract fails closed. `udp_application_wait_fails_at_bounded_
overall_deadline` replaces `server.child.wait()` with the bounded helper —
the nominal `--duration` remains runtime intent, not a harness bound.

## Gate record

- worktree: clean `git worktree` at exact `833962e`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-22T13:55:37Z → UTC end 2026-09-22T14:01:20Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
