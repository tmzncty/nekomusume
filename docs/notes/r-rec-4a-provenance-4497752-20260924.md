# R-REC-4a exact-tree provenance

**Exact pushed source/test SHA:** `4497752b9a5fc7348d322df9c81dd3659d6d5f3b`
(`test(carrier): R-REC-4a fmt` — includes `7fba25c` regression).

`aggregate_loss_single_reno_reduction` proves one ACK retiring packet 5
while threshold-declaring packets 0..=2 lost produces exactly one aggregate
`reno.lost(1200)` — `cwnd` collapses once to 6200 (after `acked(400)` grew
it 12000→12400), not once per lost packet. Loss-free ACK control
(`reno.lost(0)` no-op) preserved.

## Gate record

- worktree: clean `git worktree` at exact `4497752`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-24T02:53:09Z → UTC end 2026-09-24T02:59:10Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
