# H-I4-095 exact-tree provenance

**Exact pushed source/test SHA:** `0ed814ce4adbc6de9b803904baa665629dc2bb15`
(`test(cli): H-I4-095 ReadyProof/BarrierProof target-neutral — non-Unix cfg fix`).

`ReadyServer` holds `ready_proof` unconditionally, so `ReadyProof` (and the
pre-existing `BarrierProof`) must not be `#[cfg(unix)]`-gated — otherwise
non-Unix builds fail to compile. The zero-sized proof markers are
target-neutral (no Unix API), so the `#[cfg(unix)]` attribute was removed.

## Gate record

- worktree: clean `git worktree` at exact `0ed814c`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-22T05:50:18Z → UTC end 2026-09-22T05:56:15Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
