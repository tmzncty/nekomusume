# H-I4-113/114 exact-tree provenance

**Exact pushed source/test SHA:** `46d624e2272fcd5cf9d86457d018c4f508b8ba10`
(`test(cli): H-I4-114 duplicate-negotiation TCP read bound`).

- **H-I4-113** (`d282785` + `f5f7bba`): seven periodic-client owners
  (`--duration 5→10s`, `--duration 2→7s`), two endpoint-rebind-client owners
  (`--duration 5→10s`, `--duration 3→8s`), and matrix-probe's run closure
  (`probe --matrix`, 10s) now run under `bounded_client_output` — a live
  client can no longer strand `finish_server`.
- **H-I4-114** (`46d624e`): TCP duplicate-negotiation branch installs
  `set_read_timeout` before the first `frame_read_test` — a server that
  never responds cannot strand the test in `read_exact` before the
  close-check timeout is installed.

## Gate record

- worktree: clean `git worktree` at exact `46d624e`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0** (first run
  hit a transient `process-resource-sampler-test.py` escaped-UDP-descendant
  bind race; rerun passed clean)
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-23T14:51:06Z → UTC end 2026-09-23T14:56:50Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
