# H-I4-092 exact-tree provenance

**Exact pushed source/test SHA:** `7f678fcac75233a1eac5e59875d56e12941e15cf`
(`test(cli): H-I4-092 gated_resource_snapshot couples phase proof + measurement`).

`gated_resource_snapshot(gate, pid)` runs the phase assert inside the same
call as `process_resource_snapshot` — splitting the assert from the
measurement breaks compilation, so a refactor cannot leave a post-churn
sample guarded by a pre-churn assert (pre-churn edge) or a pre-barrier sample
guarded by the post-barrier assert (post-barrier edge).

## Gate record

- worktree: clean `git worktree` at exact `7f678fc`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-22T01:50:34Z → UTC end ~2026-09-22T01:57:00Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
