# H-I4-097 exact-tree provenance

**Exact pushed source/test SHA:** `76f33be72a1c3c24152193fb174194631cb7cf95`
(`test(cli): H-I4-097 fail-closed bounded_reap_or_kill`).

`bounded_reap_or_kill` no longer falls through to an unbounded `wait()` on a
`try_wait` `Err`, and a `kill` `Err` while the child may remain live is an
explicit panic — not a license to block. The post-kill reap is a bounded
`try_wait` poll loop (3s deadline). `malformed_classification_barrier`'s
timeout/EOF/channel-error exits share the same bounded cleanup rule.

## Gate record

- worktree: clean `git worktree` at exact `76f33be`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-22T09:51:13Z → UTC end 2026-09-22T09:57:05Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
