# H-I4-118 exact-tree provenance

**Exact pushed source/test SHA:** `71b62c642a6ea736fd162653ede9b29868e08eee`
(`fix(reliable): H-I4-118 stale ACK cannot time-threshold newer packets`).

`Recovery::on_ack` loss candidates are now bounded by `n <= largest` — a
legal duplicate/stale ACK for a retired older packet can no longer fabricate
`lost_packets`/`retransmit_frames`/congestion evidence on newer in-flight
packets merely because they aged beyond `loss_delay`. RFC 9002 §6.1
ordering: a lost packet must have been sent prior to an acknowledged packet.
`stale_ack_does_not_time_threshold_newer_packets` proves the invariant.

## Gate record

- worktree: clean `git worktree` at exact `71b62c6`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-24T04:50:06Z → UTC end 2026-09-24T04:56:00Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
