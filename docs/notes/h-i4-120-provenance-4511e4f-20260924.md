# Exact-tree check.sh provenance — H-I4-120 closure at 4511e4f (2026-09-24)

Developer-local exact-tree gate on the final pushed source/test SHA carrying
the H-I4-120 governance rollback + semantics-neutral test + erratum fix.
This is a local gate record, not hosted CI.

- **SHA:** `4511e4f147eb9511907bb4ec52cad687f113bb7a`
  (`test(reliable): H-I4-120 complete — semantics-neutral test + erratum SHA fix`,
  rebased onto `origin/main` = `6dca71c7da827b91b3bd405d218a96e555202c11`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean worktree, `git status --short` empty; `git diff --check` exit 0
- **UTC start:** ~2026-09-24T11:52:00Z (detached run; log `/tmp/check_h120b.log`)
- **UTC end:** ~2026-09-24T11:55:00Z — `EXIT=0` recorded at end of log (742 lines)
- **Exit code:** 0 (`EXIT=0`)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` stable
- **Scope:** full `scripts/check.sh` — `cargo fmt --check`, `cargo check
  --workspace --locked`, `cargo test --workspace --locked`, `cargo clippy
  --workspace --all-targets -D warnings`, plus all governance/status/era4/
  evidence/preauth/shell/release/reproducibility/smoke policy scripts and the
  bounded live warm-failover adapter tests. All passed.

## What this closes

H-I4-120 (BLOCKER): the unauthorized H-I4-119 `ack_eliciting`-only PTO-reset
patch introduced in `8d0a6e3` is rolled back to the authorized pre-gate
baseline (`pto_count` resets when the newly acked sent-packet set is
non-empty) in `9e79e6b`. This commit completes the closure:

1. The regression test is renamed `non_ack_eliciting_ack_keeps_older_packet_outstanding`
   and is semantics-neutral — it asserts only that a legal non-ack-eliciting
   ACK does not time-threshold the still-unresolved older packet, and does
   NOT pin either disputed PTO-reset outcome while H-I4-119 remains a
   maintainer/spec semantics gate.
2. The `check-gate-5c2c558-20260924.md` erratum is corrected: the rollback
   commit is `9e79e6b` (the code change), not `4f53eb8` (a provenance note).

H-I4-119 itself remains an unresolved maintainer/core ACK-PTO semantics gate;
this closure restores the authorized baseline without deciding it.
