# Exact-tree check.sh provenance — 3d2f960 (2026-09-25)

Developer-local exact-tree gate on the final reachable pushed SHA for the
close_after visible-transport-close fix in FaultInjectCarrier.

- **SHA:** `3d2f96067113a7fde8e73e0aa6b94fa98859e713`
  (`fix(carrier): close_after is a visible transport close, not a silent
  blackhole`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmCA`) at the pushed SHA,
  `git status --short` empty before and after the run; `git diff --check`
  exit 0
- **UTC start:** 2026-09-25T10:31:07Z (log captured in `/tmp/nmca-check.log`)
- **UTC end:** 2026-09-25T10:37:13Z
- **Exit code:** 0
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` stable
  (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)
- **Scope:** full `scripts/check.sh` — `cargo fmt --check`, `cargo check
  --workspace --locked`, `cargo test --workspace --locked` (36 "test result:
  ok" suites; neko-carrier lib 105/105 including the new
  `close_after_is_a_visible_transport_close`), `cargo clippy --workspace
  --all-targets --locked -- -D warnings`, plus all
  governance/status/evidence/preauth/shell/release/reproducibility/smoke
  policy scripts and bounded warm-failover adapter tests. All passed.
- **Slice scope:** fixture-repair (test-support code only, no production
  owner). close_after was a name-vs-behavior mismatch: it exercised the
  same blocked() silent-drop branch as blackhole_after with zero direct
  behavioral coverage, and the closed flag was dead state. It now models a
  real transport close: at send index N the flag sets, the inner carrier
  closes, the triggering send returns Err(CarrierError::Closed) (matching
  UdpLoopback/Tcp close semantics), subsequent sends return Closed
  idempotently, queued records drain via recv, and a withheld reorder
  record is discarded with the connection rather than silently stranded.
  This changes the transition-matrix close_after row; updated in the same
  push.
