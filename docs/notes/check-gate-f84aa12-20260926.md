# Exact-tree check.sh provenance — f84aa12 (2026-09-26)

Developer-local exact-tree gate on the final reachable pushed SHA for the second
neko-cli boundary-pin slice (multistream + periodic).

- **SHA:** `f84aa124fa2de16b23fd1906631820cd2468d054`
  (`test(cli): pin multistream bounds/hex/frame seams and periodic config/limits/ack seams`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmP2G`) created from the
  literal pushed SHA `f84aa12` (worktree head asserted equal to it);
  `git diff --check` exit 0; `git status --short` empty before and after
- **UTC start:** 2026-09-26T07:42:02Z (log `/tmp/nm-p2-gate.log`, 1072 lines)
- **UTC end:** 2026-09-26T07:48:09Z
- **Exit code:** 0 (host load average ~12.9 from unrelated jobs)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)`

## Pre-commit sequence (strictly serial)

`cargo fmt --all` / `--check` 0, `clippy --workspace --all-targets -D warnings`
0, then `cargo test --workspace`: 36 suites, 567 passed, 0 failed
(07:37:35Z-07:41:35Z, load average ~16.8). Commit, push, remote verification and
the gate were separate tool invocations; `origin/main` was fetch-verified equal
to HEAD before the gate. The gate was issued **alone**, asserting the literal
SHA.

## Slice scope

Test-only, +204 lines across two files: `crates/neko-cli/src/multistream.rs`
(+113, a new `mod tests` where the module previously had none) and
`crates/neko-cli/src/periodic.rs` (+91 added to the existing `mod tests`). No
semantics change.

Oracle: `cargo test -q -p neko-cli --bin neko-cli` (~1s incremental). 59
hand-written mutants over the two modules; **55 killed, 3 equivalent, 1 invalid
construction**:

- multistream.rs: 30 mutants, 28 killed, 2 equivalent
  (M_b_total_off, M_b_overflow_off)
- periodic.rs: 29 mutants, 27 killed, 1 equivalent (P_total),
  1 invalid construction (P_pct_divceil)

Both probe runs in the record were re-run against the final, post-clippy-fix
file content, so the kill evidence matches the committed tests exactly.

The three equivalences are unreachable-by-arithmetic rather than untested, and
two are pinned by assertions in the new tests themselves:

- periodic `P_total`: 1200 * 600 = 720_000 < 1_048_576, so `total >
  MAX_TOTAL_BYTES` can never fire (asserted in the test as both numbers).
- multistream `M_b_total_off`: 16 * 64 * 1024 = exactly 1<<20, so the filter's
  rejection branch is unreachable (the test asserts the product equals the
  ceiling and that the maximum triple is accepted).
- multistream `M_b_overflow_off`: streams/records/bytes are each range-checked
  to <= 16/<= 64/<= 1024 before the product, so the removed `checked_mul`
  cannot overflow usize and both variants always satisfy the filter.

The one invalid construction is periodic `P_pct_divceil` (a `div_floor` mutant
that does not compile for usize); rounding direction is already pinned by the
pre-existing percentile test, where 380/100 gives index 2 under floor but the
expected value 100 requires index 3 under ceil.

One in-slice correction: clippy rejected a constant-valued `assert!` on the
total-byte ceiling; it is now a `const { assert!(..) }` block, after which the
probes were re-run.

## Evidence boundary

Developer-local execution only; not reviewer-local, hosted CI, WAN, or
performance evidence. No production source changed. No H-I4-119, D019,
release-flag or policy-value change. `READY_LIVE: none`.
