# H-I4-091 exact-tree provenance

**Exact pushed source/test SHA:** `a5e8e7665041027234fd5d6ea8f125ddefe51bbc`
(`docs(manifest): failover_udp_pending/new producer-end anchor — emit after release`).

The executable/test owner is `2a36892a958028ad3ce9c8ddd2e79e7c6cf68dec`
(`fix(cli): H-I4-091 bounded post-release barrier + negative regression`);
`a5e8e76` is a docs-manifest-only commit that corrects the
`failover_udp_pending`/`failover_udp_new` `pending_producer_end` anchors to
match the post-release `emit_diagnostic` ordering already present in `main.rs`.

## Gate record

- worktree: clean `git worktree` at exact `a5e8e76`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-21T23:03:35Z → UTC end 2026-09-21T23:09:15Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
