# Exact-tree check.sh provenance — 1cc267e (2026-09-25)

Developer-local exact-tree gate on the final reachable pushed SHA for
R-MBOX-CLEAN-RECOVERY (bounded transient loss window + clean-path recovery
fixture in `FaultInjectCarrier`).

- **SHA:** `1cc267eb4dc70da053476bfd29e0dd0de96b7569`
  (`test(carrier): R-MBOX-CLEAN-RECOVERY — bounded transient loss window +
  clean-path recovery fixture`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmCR`) at the pushed SHA,
  `git status --short` empty before and after the run; `git diff --check`
  exit 0
- **UTC start:** 2026-09-25T03:50:49Z (log captured in
  `/tmp/neko-1cc267e-check.log`)
- **UTC end:** 2026-09-25T03:56:55Z
- **Exit code:** 0
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` stable
  (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)
- **Scope:** full `scripts/check.sh` — `cargo fmt --check`, `cargo check
  --workspace --locked`, `cargo test --workspace --locked` (36 "test result:
  ok" suites; neko-carrier lib 96/96 including the three new deterministic
  regressions: `loss_window_drops_bounded_burst_then_clean_path`,
  `loss_window_drop_is_drop_class_not_reorder_class`,
  `clean_recovery_after_bounded_loss_replays_uncertain_without_duplicates`),
  `cargo clippy --workspace --all-targets --locked -- -D warnings`, plus all
  governance/status/evidence/preauth/shell/release/reproducibility/smoke
  policy scripts and bounded warm-failover adapter tests. All passed.
- **Semantics implemented (fixture-only):** `loss_window: Option<(u64, u64)>`
  on `FaultPolicy` — sends whose index falls in `[start, end)` are dropped
  (bounded transient burst); the path is clean again from index `end`.
  Unlike `blackhole_after` (permanent) or `one_way` (permanent alternation),
  this models controlled loss followed by a clean path. Window drops are
  drop-class: never enter the bounded reorder buffer, never release a
  withheld record, never duplicated. The end-to-end regression uses ONLY
  existing recovery semantics (track_uncertain / confirm /
  PTO-threshold failover / tcp_resend / exact-duplicate receive dedup);
  no new retransmission/timer/readiness policy; H-I4-119 untouched.
