# Exact-tree check.sh provenance — c389ec6 (2026-09-25)

Developer-local exact-tree gate on the final reachable pushed SHA for the
self-directed reorder swap-atomicity fix in FaultInjectCarrier.

- **SHA:** `c389ec66966d2debfe845169c8263c47bfc5f304`
  (`fix(carrier): reorder swap atomicity — restore pending on delivery
  failure`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmSA`) at the pushed SHA,
  `git status --short` empty before and after the run; `git diff --check`
  exit 0
- **UTC start:** 2026-09-25T08:59:32Z (log captured in `/tmp/nmsa-check.log`)
- **UTC end:** 2026-09-25T09:05:38Z
- **Exit code:** 0
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` stable
  (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)
- **Scope:** full `scripts/check.sh` — `cargo fmt --check`, `cargo check
  --workspace --locked`, `cargo test --workspace --locked` (36 "test result:
  ok" suites; neko-carrier lib 102/102 including the new
  `reorder_swap_failure_restores_pending_atomically`), `cargo clippy
  --workspace --all-targets --locked -- -D warnings`, plus all
  governance/status/evidence/preauth/shell/release/reproducibility/smoke
  policy scripts and bounded warm-failover adapter tests. All passed.
- **Slice scope:** fixture-repair (test-support code only, no production
  owner). The reorder swap previously took the withheld record out of the
  pending slot before delivering both halves; if the inner carrier
  rejected the second deliver (BufferFull/Closed/PeerClosed), the withheld
  record was silently dropped while the slot appeared empty. Fix restores
  the withheld record to the slot on second-half failure: a swap either
  delivers both halves or errors with the record retained for retry —
  never a silent loss. This changes FaultInjectCarrier fixture semantics
  documented in `docs/notes/r-mbox-transition-matrix-20260925.md` (the
  reorder row's "deterministic behavior" wording gains the
  failure-atomicity clause); updated in the same push.
