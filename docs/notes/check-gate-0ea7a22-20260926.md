# Exact-tree check.sh provenance — 0ea7a22 (2026-09-26)

Developer-local exact-tree gate on the final reachable pushed SHA for the
ConcurrentCarrierManager limit/register/readiness/drain/event-ring pins.

- **SHA:** `0ea7a22c207eafd4f3dff5baf8a8481948e53947`
  (`test(carrier): pin ConcurrentCarrierManager limits, register, readiness, drain scope and event ring`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmCC`) created from
  `git rev-parse origin/main` (asserted equal to local HEAD and worktree HEAD);
  `git diff --check` exit 0; `git status --short` empty before and after
- **UTC start:** 2026-09-26T02:23:13Z (log `/tmp/nmcc-check.log`)
- **UTC end:** 2026-09-26T02:29:22Z
- **Exit code:** 0 (only gate run on this SHA; host load average ~26 from unrelated jobs)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)

## Pre-commit sequence (strictly serial)

fmt --check 0, `clippy --workspace --all-targets -D warnings` 0, then
`cargo test --workspace` with no background mutation run active:
36 suites, 500 passed, 0 failed (first run). Then commit → push → fetch
(`origin/main` == HEAD) → this gate.

## Slice scope

Test-only, no semantics change. 50 mutants over ConcurrentCarrierManager
(dwell/cooldown/warm-cold classification excluded as previously pinned).
26 survived the neko-carrier lib tests; 25 of those also survived the full
neko-carrier package (16 suites / 161 tests, every run complete). Six new
tests in `concurrent_manager_tests` kill 19 (re-verified per mutant). Of the
remaining 7, P1 (`replay_uncertain` replays every range) is killed by the
neko-carrier integration tests; the other 6 are argued equivalent or
unreachable in the commit message (O7 ready clamp, A2/F2 activate/fail
capacity pre-check, F6 fail path pre-check, D4 confirmed-range replay,
C3 mark-all-uncertain). The only other consumer of
ConcurrentCarrierManager is neko-observe; no cross-crate re-run was needed
because no killable survivor was left untested.

## Correction to the 0ea7a22 commit message

The A2/F2 bullet says "and C1-class bounds" are unreachable. That is
inaccurate: the C1/C2 mutants (`count >= max_uncertain_ranges`,
`bytes >= max_uncertain_bytes` in `ensure_uncertain_capacity`) were
**killed** by the existing
`uncertain_capacity_conversion_bounded_at_assign_time`, which converts
exactly at both bounds. Only the removal of the pre-check (A2, F2) is
unreachable through the public API.

## Evidence boundary

Developer-local execution only; not reviewer-local, hosted CI, WAN, or
performance evidence. No H-I4-119, D019, release-flag or policy-value change.
`READY_LIVE: none`.
