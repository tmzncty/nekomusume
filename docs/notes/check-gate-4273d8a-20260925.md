# Exact-tree check.sh provenance — 4273d8a (2026-09-25)

Developer-local exact-tree gate on the final reachable pushed SHA for the
time-threshold loss-delay contract pin.

- **SHA:** `4273d8a0d6791eaa2063d462c94936c4c1cda2ad`
  (`test(reliable): pin time-threshold loss delay (9/8 ceil of max latest/smoothed) and inclusive boundary`)
- **Command:** `./scripts/check.sh` (run WITHOUT `PYTHONDONTWRITEBYTECODE=1`)
- **Tree state:** clean detached worktree (`/tmp/nmLD`) at the pushed SHA;
  `git diff --check` exit 0; `git status --short` empty before the run.
  After the run the only entries were untracked `scripts/__pycache__/` and
  `scripts/bench/__pycache__/` (Python bytecode caches from the policy
  scripts, because the bytecode-suppression env var was not set); no tracked
  file changed. Future runs should set `PYTHONDONTWRITEBYTECODE=1`.
- **UTC start:** 2026-09-25T13:11:27Z (log captured in `/tmp/nmld-check.log`)
- **UTC end:** 2026-09-25T13:17:32Z
- **Exit code:** 0
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)
- **Scope:** full `scripts/check.sh` (fmt, check, workspace tests, clippy
  `-D warnings`, governance/status/evidence/policy scripts, bounded
  warm-failover adapter tests). Pre-commit local workspace run: 36 suites,
  460 tests passed, 0 failed; neko-reliable lib 38/38 including the two new
  tests.
- **Slice scope:** test-only, no semantics change. `RttEstimator::loss_delay_us`
  (spec `docs/spec/m2-udp-recovery.md`: 9/8 of max(latest RTT, smoothed RTT))
  had no direct test. Local mutation probing on the pre-slice tree showed
  five surviving mutants: smoothed-only base, latest-only base, min instead
  of max, floor instead of ceiling division, and strict `>` instead of
  inclusive `>=` at the time-threshold boundary in `Recovery::on_ack`
  (only the 9/8 → 1 ratio mutant was already killed). New tests
  `loss_delay_is_ceiling_nine_eighths_of_max_latest_smoothed` and
  `time_threshold_loss_boundary_is_inclusive` kill all five (re-verified by
  re-applying each mutant: 37/38, one named failure each).
- **Evidence boundary:** developer-local execution only; not reviewer-local,
  hosted CI, WAN, or performance evidence. Does not touch H-I4-119 (PTO reset
  semantics gate), D019, release flags or policy values. `READY_LIVE: none`.
