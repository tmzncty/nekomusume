# Exact-tree check.sh provenance — c55d0c8 (2026-09-26)

Developer-local exact-tree gate on the final reachable pushed SHA for the
FairScheduler limit/order pins.

- **SHA:** `c55d0c867be689026f31752083c2ce860bc3a4c2`
  (`test(carrier): pin FairScheduler limits, idempotent reopen, FIFO and round-robin order`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmSCH`) created from
  `git rev-parse origin/main` and asserted equal to HEAD; `git diff --check`
  exit 0; `git status --short` empty before and after the run
- **UTC start:** 2026-09-25T22:56:54Z (log captured in `/tmp/nmsch-check.log`)
- **UTC end:** 2026-09-25T23:03:04Z
- **Exit code:** 0 (single run; host load average ~23 at start from unrelated jobs)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)
- **Scope:** full `scripts/check.sh`. Pre-commit local workspace run: 36
  suites, 486 tests passed, 0 failed.
- **Slice scope:** test-only, no semantics change. Mutation probing on
  `99fa635` of `FairScheduler` (20 mutants) found 13 survivors under the
  neko-carrier lib tests; all 13 also survived the neko-cli and neko-observe
  suites that construct `FairScheduler`. New tests
  `scheduler_limits_are_exact_and_reopen_is_idempotent` and
  `scheduler_is_fifo_per_stream_and_round_robin_across_streams` kill all 13
  (re-verified per mutant): zero `FlowLimits` fields rejected; idempotent
  reopen preserving the queue; inclusive `max_streams`, per-stream and
  session byte limits; empty data rejected; dequeue releasing session budget;
  snapshot priority/queued/max bytes; per-stream FIFO and cross-stream
  round-robin cursor advance. No equivalent mutants remained.
- **Evidence boundary:** developer-local execution only; not reviewer-local,
  hosted CI, WAN, or performance evidence. No H-I4-119, D019, release-flag or
  policy-value change. `READY_LIVE: none`.
