# H-I4-111 exact-tree provenance

**Exact pushed source/test SHA:** `1d727f065436fbe15ff9151e48a746da9fb2383b`
(`test(cli): H-I4-111 remaining probe client ownership bounded`).

- `first_udp_selection_loss_recovers_from_same_peer_duplicate_hello`'s
  concurrent-injection client uses `bounded_wait_with_output(9s)` —
  `--duration 4 + 5s` under the established duration-plus-slack convention.
- `expired_preprogress_udp_session_is_retired_before_delivery_and_fresh_
  handshake_recovers`'s sequential expired (`--duration 2 → 7s`) and recovery
  (`--duration 3 → 8s`) clients use `bounded_client_output`.
- `warm_readiness_failures_close_before_admission_or_application_data`'s
  `failover-client` (`--duration 2 → 7s`) and `server.wait_with_output` are
  bounded too — a live client/server can no longer strand the gate.

## Gate record

- worktree: clean `git worktree` at exact `1d727f0`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-23T05:51:19Z → UTC end 2026-09-23T05:57:00Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
