# Exact-tree check.sh provenance — 654ead8 (2026-09-26)

Developer-local exact-tree gate on the final reachable pushed SHA for the
neko-carrier FailoverController mutation-pin slice.

- **SHA:** `654ead807c2e3fcec6cbf1dcc4d10711985c6ec9`
  (`test(carrier): pin FailoverController limits, escalation clock and decision gates`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmFOG`) created from
  `git rev-parse origin/main` (asserted equal to local HEAD, to the literal
  pushed SHA, and to worktree HEAD); `git diff --check` exit 0;
  `git status --short` empty before and after
- **UTC start:** 2026-09-26T05:17:51Z (log `/tmp/nmfo-gate.log`)
- **UTC end:** 2026-09-26T05:23:58Z
- **Exit code:** 0 (host load average ~22.5 from unrelated jobs)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)` (`cargo 1.98.0 (797e8a9bc 2026-08-05)`)

## Pre-commit sequence (strictly serial)

fmt --check 0, `clippy --workspace --all-targets -D warnings` 0, then
`cargo test --workspace`: 36 suites, 535 passed, 0 failed
(05:09:29Z-05:17:27Z, load average ~26), no re-run needed. Commit, push,
remote verification and the gate were four separate tool invocations;
`origin/main` was fetch-verified equal to HEAD before the gate was issued.

## Slice scope

Test-only, +4 tests in `crates/neko-carrier/src/lib.rs`
(`mod failover_controller_boundary_tests`), no semantics change. 37
hand-written mutants over FailoverController::new, track_uncertain,
udp_progress, udp_pto_at, apply_manager_decision, record_recovery_latency,
apply_migration_back, tcp_resend, confirm and receive were probed with the
full neko-carrier package (16 suites, 181 tests) as the oracle: 37 killed,
0 survivors, 0 equivalents (the only COMPILE_ERR candidate was replaced
with an equivalent-shaped mutation that compiles).

The canonical outcomes asserted by the new tests were confirmed first in a
throwaway probe module inside the scratch worktree (`/tmp/nmFO`, not
committed).

## Evidence boundary

Developer-local execution only; not reviewer-local, hosted CI, WAN, or
performance evidence. No production source changed. No H-I4-119, D019,
release-flag or policy-value change. `READY_LIVE: none`.
