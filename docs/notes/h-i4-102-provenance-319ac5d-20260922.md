# H-I4-102 exact-tree provenance

**Exact pushed source/test SHA:** `319ac5d8caa7c245c35e4477bb3cf707ffb4fee2`
(`test(cli): H-I4-102 bounded_reap_or_kill try_wait-Err converges ownership`).

`bounded_reap_or_kill`'s own initial `try_wait` `Err` now runs
`bounded_kill_reap` before panicking — the same ownership rule as the
H-I4-101 caller-side repairs. Three states kept truthful: `Ok(Some)` exit
proven, `Ok(None)` live → bounded kill+reap, `Err` observation failed →
bounded best-effort kill+reap then explicit failure.

## Gate record

- worktree: clean `git worktree` at exact `319ac5d`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-22T15:50:41Z → UTC end 2026-09-22T15:56:25Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
