# Exact-tree check.sh provenance — 2233094 (2026-09-26)

Developer-local exact-tree gate on the final reachable pushed SHA for the
neko-session datagram-queue / observable-event boundary-pin slice.

- **SHA:** `22330940dbd32f7be185c429db0b32cefd446b20`
  (`test(session): pin datagram queue order, counter classes and event-field identity`)
- **Command:** `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`
- **Tree state:** clean detached worktree (`/tmp/nmSG`) created from the
  literal pushed SHA `2233094` (worktree head asserted equal to it);
  `git diff --check` exit 0; `git status --short` empty before and after
- **UTC start:** 2026-09-26T13:49:22Z (log `/tmp/nm-sess-gate.log`, 1141 lines)
- **UTC end:** 2026-09-26T13:55:33Z
- **Exit code:** 0 (host load average ~10.6 from unrelated jobs)
- **OS / arch:** Linux 6.8.0-137-generic, GNU/Linux (x86_64)
- **Rust toolchain:** `rustc 1.98.0 (88d9e12ae 2026-08-18)`

## Pre-commit sequence (strictly serial)

`cargo fmt --all --check` 0, `clippy --workspace --all-targets -D warnings` 0,
then `cargo test --workspace`: 38 suites, 624 passed, 0 failed
(13:44:53Z-13:48:58Z, load average ~9.8). Commit, push, remote verification and
the gate were separate tool invocations; `origin/main` was fetch-verified equal
to HEAD before the gate. The gate was issued **alone**, asserting the literal
SHA.

## Slice scope

Test-only, additions only: +114 lines in `crates/neko-session/src/lib.rs` (six
tests: four in `datagram_runtime_tests`, two in `runtime_tests`). No semantics
change.

33 hand-written mutants over the bounded unreliable `DatagramRuntime`
(new/counters/queued/close/send/receive), `ObservableEvent::to_json_line` and
`RuntimeEventKind::name`; **33 killed, 0 survivors, 0 HANGs, 0 invalid
constructions, 0 BADANCHOR**. There is **no residual to report for this slice** -
every seam these tests can reach is pinned. Baseline measured green in a
separate worktree (`/tmp/nmSb`); the pre-existing tests alone left 9 survivors.

Two of the gaps were shape traps of a kind seen in earlier slices:

- **FIFO order was unasserted**: every existing test used `max_queue` of 1, so
  `push_front`/`push_back` and `pop_front`/`pop_back` were indistinguishable.
  The new test queues three records into a four-slot queue and reads them back
  in order.
- **The observable-event JSON test was a symmetric fixture**: it asserted the
  full exact line, but with `seq` and `at_ms` both zero, so swapping the two
  format arguments was invisible. That mutant survived on exactly this, which is
  how it was found. The new case uses `seq` 1 against `at_ms` 3.

Also newly pinned: the counter classes are not conflated (`rejected_oversize` is
deliberately outside the generic `dropped`, while a queue-full refusal
increments both `queue_dropped` and `dropped` - the invariant the observer layer
relies on), `queued()`, `close()` discarding the queue, the closed-send drop
accounting and its dedicated error variant, `new()`'s zero cases reporting the
dedicated variant rather than just being an error, and `RuntimeEventKind::Error`
being produced and named exactly (plus all kind names being mutually distinct).

One in-slice correction: the first JSON test used `at_ms` 500, which exceeds the
runtime's idle timeout and was correctly rejected with `IdleTimeout`; it now uses
3. Because the file changed, the whole probe was re-run against the corrected,
formatted content and the pre-probe snapshot diffed clean against the committed
file (absolute paths). The results recorded here are that final run.

## Evidence boundary

Developer-local execution only; not reviewer-local, hosted CI, WAN, or
performance evidence. No production source changed. No H-I4-119, D019,
release-flag or policy-value change. `READY_LIVE: none`.
