# H-I4-115 exact-tree provenance

**Exact pushed source/test SHA:** `1f6d744c9954b9a7a5d693b43a20d137a5c5e75d`
(`test(cli): H-I4-115 descendant-held pipe bound in bounded_wait_with_output`).

`bounded_wait_with_output` (probe.rs + multistream.rs) routes drained
stdout/stderr buffers through bounded `mpsc` channels; after direct-child
exit/reap, reader completion has a 3s deadline — a descendant holding an
inherited writer produces an explicit finite harness failure, not an
unbounded `join()`. Unix regression
`bounded_client_output_fails_when_descendant_holds_pipe_open` challenges the
inherited-writer premise with a finite descendant.

## Gate record

- worktree: clean `git worktree` at exact `1f6d744`
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh` — **exit 0**
- `git diff --check` — **exit 0**
- `git status --short` — clean
- UTC start 2026-09-23T16:51:02Z → UTC end 2026-09-23T16:56:45Z
- OS/arch: Linux x86_64
- Rust: rustc 1.98.0 (88d9e12ae 2026-08-18), stable

This is developer-reported local provenance, not reviewer-local execution or
hosted CI. No decoder/parser/crypto framing change — no fuzz required. No
release flag, D019, or policy value touched. `READY_LIVE: none`.
