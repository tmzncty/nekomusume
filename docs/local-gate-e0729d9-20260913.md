# Local exact-tree gate — `e0729d9` (Session-above-Carrier layering restored)

## What this tree changes

Exact `e0729d96f59988e7b918d21cfb19579c319b0733` is the pushed developer source/test tree
that closes reviewer finding **H-RUDP-012** and finishes the remaining **H-RUDP-011**
ownership-contract details.

- Production `neko-carrier` no longer owns Session logical delivery state: `deliver_logical`,
  the unbounded `delivered` map and `dedup_suppressed` are gone, and the crate keeps its
  carrier-local production dependencies (`neko-reliable`, `neko-wire`). `neko-session`
  remains a dev-dependency only.
- The R7 integration fixture composes a real bounded `neko_session::SessionRuntime` instead:
  the stream is opened explicitly, an exact logical duplicate is suppressed by Session
  semantics and counted, and conflicting bytes for one logical identity fail closed with the
  error retained rather than swallowed by `let _`.
- `PathRecoveryError::Retransmit(RetransmitError)` preserves the capacity/oversize/conflict
  distinction instead of collapsing every plaintext-owner refusal into one error.
- A failed packet attempt rolls back only a plaintext copy reserved by that same call, so an
  identical frame retained earlier survives the failure.
- `on_retransmit_sent` fails before recording a packet unless the stable frame still has
  retained resealable plaintext ownership.
- `teardown()` drops the packet->frame map and the one bounded plaintext owner together.
- New regressions: atomic typed refusals (oversize, conflict), rollback of only the new
  reservation, preservation of a pre-existing identical frame, retransmit-without-ownership
  refusal, deterministic teardown, and the fixture-level Session duplicate/conflict flow.
- No packet-recovery or ACK path marks Session delivery; packet feedback remains separate.

## Verified exact-tree gate

Executed on a clean detached checkout of the exact pushed SHA
`e0729d96f59988e7b918d21cfb19579c319b0733`:

- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`: exit `0`
- `git diff --check`: exit `0`
- initial source tree clean: `true`; final source tree: empty (`clean`)
- UTC `2026-09-13T15:03:20Z` -> `2026-09-13T15:05:19Z`
- host `Linux 6.8.0-137-generic x86_64`
- `rustc 1.98.0 (88d9e12ae 2026-08-18)`

Focused developer evidence on the same work: `cargo test --workspace --locked` all targets
green (carrier lib 69 tests including the four new ownership regressions and the Session
layering fixture), `cargo clippy --workspace --all-targets --locked` warning-free.

No wire decoder/parser/framing code changed, so no decode fuzz rerun is claimed or required
for this slice.

## Superseding classification of the earlier provenance note

`docs/local-gate-eeb49d7-20260913.md` remains **valid green exact-tree provenance for
`eeb49d7`** (that tree did build and test cleanly). Its *semantic* sentence describing
`eeb49d7` as the final tree after closing the reviewer's second-round R7 findings is
**superseded**: H-RUDP-011 and H-RUDP-001D were still present at that exact source, and
H-RUDP-012 was introduced by `1e53ffe`. The historical file is intentionally not rewritten.

## Scope of this closure (not claimed)

This closes the layering/ownership repair only. It is **not** an accepted R7 closure, not an
independent review, and not a release decision. Still open: the coherent R7 fixture scenario
set (Q4 remainder), reviewer re-review of the repaired ownership/layering (Q7), R8 CLI/lab
integration, R9 process/socket acceptance, the separate `SessionRuntime.events`
retained-state policy, D019 source retention, RSEC-001 representative
adversarial-load/capacity suitability, signing/key-custody/SBOM, previous-frozen-release
interoperability, item 3, item 4, and RC/freeze/release/production authority.

Unchanged: `RELEASE_CANDIDATE=false`, `PRODUCTION_READY=false`, `FREEZE=false`,
`RELEASED=false`, `READY_LIVE: none`.
