# Developer-local exact-tree provenance — `94cfd47` live reliable-UDP integration S1–S9

Exact pushed source SHA: `94cfd4732dca6aae3e88fd188125653d9b44e900` (`style(carrier): bool assert for clippy clean in auth test`), the coherent S1–S9 live reliable-UDP integration tree.

Developer-local clean exact-tree gate. Hosted GitHub checks are separate cross-evidence; this is the developer-local anchor. Not a WAN/live claim, not an independent security/release approval.

## Commands run on a clean worktree of the exact SHA

```bash
cargo test -p neko-carrier -p neko-wire -p neko-reliable   # plus workspace
PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh
git diff --check
git status --short
```

## Result

- UTC start `2026-09-13T10:19:23Z`, UTC end `2026-09-13T10:21:20Z`.
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`: exit 0 (includes `cargo test` workspace, clippy `-D warnings`, markdown-link and process runners; plus `live warm failover adapter`, `repeated warm failover`, `remote endpoint verifier`, `periodic command` tests all green).
- `git diff --check`: exit 0.
- `git status --short` before/after: empty.
- Nightly decode fuzz smoke (`cargo fuzz build/run decode -max_total_time=30 -max_len=8192`): run earlier on the S2 wire tree — ~7M runs clean over `decode`, `decode_ack`, `decode_packet`, `ProcessMessage`, negotiation.
- Host `Linux 6.8.0-137-generic x86_64 GNU/Linux`; `rustc 1.98.0 (88d9e12ae 2026-08-18)`; Cargo.lock updated for `neko-carrier` deps `neko-reliable` + `neko-observe`.

`READY_LIVE: none` and all release flags are unchanged; S1–S9 is local/loopback integration only.
