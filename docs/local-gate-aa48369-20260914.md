# Developer-local exact-tree provenance — `aa48369` R9 reliable-UDP failover seam

Exact pushed source SHA: `aa483694b83e554fd802bee1250c32162e20106e` (`feat(cli): R9 cross-process reliable-UDP on the failover seam`).

Developer-local clean exact-tree gate. Not a WAN/live claim, not an independent security/release approval.

## Gate (clean detached worktree of exact SHA)

```bash
PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh   # exit 0
git diff --check                                  # exit 0
git status --short                                # empty before/after
```

- UTC start `2026-09-13T21:26:07Z`, UTC end `2026-09-13T21:28:06Z`.
- Host `Linux 6.8.0-137-generic x86_64 GNU/Linux`; `rustc 1.98.0`.

## R9-1/R9-2 landed

`failover --role client|server --reliable-udp` reuses the existing failover
process seam (UDP listener + TCP listener + Noise + SessionRuntime +
readiness/warm promotion) and routes post-auth UDP application Data through
`ReliableUdpRuntime`:

- client: `can_send` admission before Session offset/recovery commit; packet
  number = authenticated outer AEAD sequence; `on_packet_sent` records stable
  `FrameId` = Session byte offset; `recv_udp_delivery_ack` feeds packet ACKs
  to `apply_ack` while awaiting the separate Session `DeliveryAck`; bounded
  settlement drains to `in_flight()==0` (`r9_udp_in_flight_settled`).
- server: `on_packet_received` + `poll_outgoing_ack` emits an authenticated
  Carrier packet ACK (`seal_unreliable`) alongside the existing Session
  `DeliveryAck`; Session dedup stays in `SessionRuntime`, never Carrier-owned.
- no-loss cross-process test: `remaining_in_flight=0`, `udp_packet_ack_sent`,
  `udp_delivery_ack_sent`, cleanup verified; default failover unchanged
  without `--reliable-udp`.

`READY_LIVE: none` unchanged; no WAN/VPS claim.
