# Developer-local exact-tree provenance — `3739138` R1–R4 repairs

Exact pushed source SHA: `373913800162b99e6410270f209642b3a128a256` (`fix(carrier): per-packet charged-bytes accounting for congestion (M-RUDP-004)`), the tree closing the reviewer's H-RUDP-001 HIGH plus M-RUDP-002/003/004.

Developer-local clean exact-tree gate. Hosted GitHub checks are separate cross-evidence; this is the developer-local anchor. Not a WAN/live claim, not an independent security/release approval.

## Commands run on a clean worktree of the exact SHA

```bash
PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh
git diff --check
git status --short
```

## Result

- UTC start `2026-09-13T11:05:35Z`, UTC end `2026-09-13T11:07:32Z`.
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`: exit 0.
- `git diff --check`: exit 0.
- `git status --short` before/after: empty.
- Host `Linux 6.8.0-137-generic x86_64 GNU/Linux`; `rustc 1.98.0 (88d9e12ae 2026-08-18)`.

## Repairs closed in this tree

- **H-RUDP-001** (`16c17f6`): fresh-evidence health bridge — `fresh_health_sample()` yields a `HealthSample` only when `evidence_epoch` advanced; repeated no-evidence polls return `None`, so one cumulative loss snapshot can no longer be replayed into several bad observations.
- **M-RUDP-002** (`badaafd`): single packet-number source — the authenticated `SecureSession` record sequence (AEAD nonce); removed the unused `PathRecovery` allocator/`next_packet_number`.
- **M-RUDP-003** (`9890bc0`): replaced the vacuous `|| true` adjacent-range assertion with real encoder `NonCanonicalRanges` and raw-overlapping `decode_ack` rejections.
- **M-RUDP-004** (`3739138`): per-packet charged-bytes accounting — a non-ack-eliciting packet is recorded but charged 0, so acking/losing it can no longer drain another packet's congestion bytes.

`READY_LIVE: none` and all release flags are unchanged.
