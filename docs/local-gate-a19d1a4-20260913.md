# Developer-local exact-tree provenance — `a19d1a4` R7 orchestrator + BLOCKER fix

Exact pushed source SHA: `a19d1a4e719a4b83ddba941ad9f24732ce7ac349` (`style(carrier): rustfmt manager.fail chain in runtime test (BLOCKER-GATE-001)`), the tree after the `ReliableUdpRuntime` orchestrator plus the stable-gate repair.

Developer-local clean exact-tree gate. Hosted GitHub checks are separate cross-evidence; this is the developer-local anchor. Not a WAN/live claim, not an independent security/release approval.

## Commands run on a clean worktree of the exact SHA

```bash
PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh
git diff --check
git status --short
```

## Result

- UTC start `2026-09-13T12:48:03Z`, UTC end `2026-09-13T12:50:00Z`.
- `PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh`: exit 0 (the `a20eb72` hosted-stable red was a `cargo fmt --check` miss on `reliable_udp_runtime.rs:200` — the `manager.fail` chain — now green).
- `git diff --check`: exit 0.
- `git status --short` before/after: empty.
- Host `Linux 6.8.0-137-generic x86_64 GNU/Linux`; `rustc 1.98.0 (88d9e12ae 2026-08-18)`.

## State

`ReliableUdpRuntime` is present but the reviewer's second-round findings remain OPEN and under repair: H-RUDP-007 (retransmit/FrameId ownership), H-RUDP-001C (cumulative-loss replay across fresh outcomes), H-RUDP-008 (stable Session identity/dedup), M-RUDP-009 (real cwnd/pacing send gate), M-RUDP-010 (explicit fallback result). `READY_LIVE: none`; all release flags unchanged.
