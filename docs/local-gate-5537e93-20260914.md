# Developer-local exact-tree provenance — `5537e93` R8 reliable-UDP lab

Exact pushed source SHA: `5537e93fb31e3d2dd2f14186b1346df27cd070c1` (`feat(cli): lab --reliable-udp bounded executable reliable-UDP path (R8)` + Cargo.lock).

Developer-local clean exact-tree gate. Not a WAN/live claim, not an independent security/release approval.

## Gate (clean detached worktree of exact SHA)

```bash
PYTHONDONTWRITEBYTECODE=1 bash scripts/check.sh   # exit 0
git diff --check                                  # exit 0
git status --short                                # empty before/after
```

- UTC start `2026-09-13T16:36:48Z`, UTC end `2026-09-13T16:38:46Z`.
- Host `Linux 6.8.0-137-generic x86_64 GNU/Linux`; `rustc 1.98.0`.

## What R8 exposes

`neko lab --reliable-udp --rounds N --drop-every M [--json]` — a real loopback
UDP socket pair driven by the committed `ReliableUdpRuntime` with a real
`SecureSession`. Deterministic sender-side packet suppression creates
controlled loss (NOT natural Internet loss); receiver emits `take_ack` ACKs;
PTO retransmits under fresh packet numbers with stable `FrameId`; application
delivery dedups via the committed `SessionRuntime` (Session dedup, never
Carrier). Fresh interval-delta health crosses hysteresis and auto-promotes the
ready warm TCP standby (`WarmFallback`/`active_tcp`).

Observed sample run (`--rounds 16 --drop-every 5 --json`):

```json
{"ok":true,"mode":"reliable-udp","packets_sent":16,"suppressed":3,"retransmits":58,"acks_sent":69,"delivered":4,"duplicates":65,"fallback":true,"active_tcp":true}
```

Bounded: `--rounds` 4-64, `--drop-every` 2-16, loopback-only, no listener, no
daemon. `READY_LIVE: none` unchanged.
