# Exact `60cd40d` periodic Session attempt

Exactly one live invocation was made; retry count was zero. It ended before client dispatch with `start_timeout`: SSH server transport exit 255, `periodic_client_entered=false`, client `not_started`, and no application metrics. The artifact's own cleanup collection is failed and remains unchanged. A later cleanup-only observation separately found zero local/remote experiment processes, listeners, temporary paths, and deployment residue; that observation does not rewrite the artifact cleanup fields.

This is a pre-application orchestration negative, not a Session runtime failure or reliability sample. Endpoint addresses remain redacted and private streams are not tracked.
